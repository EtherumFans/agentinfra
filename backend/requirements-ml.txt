# Optional semantic retrieval worker dependencies.
# Install only in an isolated Linux worker after an SBOM/vulnerability review.
# Do not install these packages in the API image or the Windows Codex process.
sentence-transformers==5.7.0
faiss-cpu==1.15.0
