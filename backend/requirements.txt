# iCoDer development and CI dependencies (Python 3.11+).
# The deployable API surface is intentionally kept in requirements-api.txt.
-r requirements-api.txt

# Testing
pytest==9.0.3
pytest-asyncio==1.4.0
pytest-cov==5.0.0
requests==2.33.0
# Starlette 1.3 TestClient uses the maintained HTTPX2 backend.  Keep the
# production-facing httpx client pinned separately in requirements-api.txt.
httpx2==2.12.0
