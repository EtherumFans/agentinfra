# 5分钟快速开始
1. 启动: `python -m uvicorn app.main:app --port 8000`
2. 创建Agent: http://localhost:5173/ai-studio/agents → 新建
3. 安装到Runtime: Agent详情页 → Install to Runtime
4. 运行: POST /api/runtime/agents/{ref}/run
5. 发布: 上架到Marketplace
