# Corti × iCoDer 当前能力差距矩阵（更新至 2026-08-27）

本文件是当前状态的权威差距摘要；较早的 `ICODER_AUDIT_GAP_MATRIX.md` 和 2026-07-07 矩阵保留为历史审计记录，不应覆盖本文件已经验证的后续证据。

2026-08-27 的合成 shadow observation、停止策略与自动回滚见 [`ICODER_CLINICAL_MODEL_SHADOW_OBSERVATION_PHASE_SUMMARY_2026-08-27.md`](ICODER_CLINICAL_MODEL_SHADOW_OBSERVATION_PHASE_SUMMARY_2026-08-27.md)：Alembic `060` 增加组织级聚合评估与 binding 三态门禁；已验签合成包执行三轮隔离观察，受控超时、畸形响应和模型摘要漂移均停止，存在 previous binding 时按事务自动回滚并单独审计。该链路不接收患者数据、不保存输入、不输出预测、不联网且不改变生产 Runtime；开关默认关闭并在 cloud/production 拒绝。聚焦后端 **13/13**、JS **97/97**、Python **103/103**、.NET net8/net10 各 **85/85**、前端 **166/166**、预检 **114/114**；SDK 为 `beta.46/b46`，未发布。这关闭开发合成 observation/停止/回滚状态机，不代表真实 shadow traffic、真实模型临床质量、分布式生产回滚或 Corti 等价。

2026-08-27 的临床模型 artifact 供应链与 shadow-only 边界见 [`ICODER_CLINICAL_MODEL_SHADOW_SUPPLY_CHAIN_PHASE_SUMMARY_2026-08-27.md`](ICODER_CLINICAL_MODEL_SHADOW_SUPPLY_CHAIN_PHASE_SUMMARY_2026-08-27.md)：Alembic `059` 新增组织级 metadata-only attestation 与 shadow binding；仓库合成 bundle 经过 Ed25519 开发签名、CycloneDX SBOM、恶意 ZIP 防护、内容扫描与 `python -I -S` 有界子进程 2/2 聚合探针，上传 ZIP/model 不落库。开关默认关闭且 cloud/production 强制拒绝；shadow 绑定不接收患者数据、不输出预测、不改变 Runtime。聚焦后端 **23/23**、JS **97/97**、Python **103/103**、.NET net8/net10 各 **84/84**、前端全量 **164/164**、预检 **113/113**；三 SDK 为 `beta.45/b45`，未发布。这关闭开发合成 artifact 的签名/扫描/证明/选择合同，不代表生产 AV/KMS/对象存储/OS sandbox、真实模型、真实 shadow traffic、Corti 托管 Models 或临床等价。

2026-08-27 的临床模型包治理控制面见 [`ICODER_CLINICAL_MODEL_PACKAGE_GOVERNANCE_PHASE_SUMMARY_2026-08-27.md`](ICODER_CLINICAL_MODEL_PACKAGE_GOVERNANCE_PHASE_SUMMARY_2026-08-27.md)：Alembic `058`、组织级 metadata-only manifest、不可变版本、四眼审批、乐观锁激活、切换与严格 previous-version rollback 已覆盖 API、OpenAPI、Console 和三 SDK；许可、再分发、独立 gold/reviewer、医院与云授权缺一即失败关闭，且 `runtime_loading_enabled=false`。CCL 同工作簿 OOF 包的反例回归明确返回五类外部门禁阻断，没有通过登记绕过安全边界。聚焦后端 **24/24**、JS **96/96**、Python **102/102**、.NET net8/net10 各 **83/83**、前端合同 **77/77**、预检 **112/112**；三 SDK 为 `beta.44/b44`，未发布。这关闭组织级模型治理控制面差距，不代表模型已加载、Corti 托管 Models 等价、独立临床验证或医院生产批准。

2026-08-27 的 CCL 2026 本地监督 OOF 基线见 [`ICODER_CCL2026_LOCAL_SUPERVISED_OOF_PHASE_SUMMARY_2026-08-27.md`](ICODER_CCL2026_LOCAL_SUPERVISED_OOF_PHASE_SUMMARY_2026-08-27.md)：以授权且治理绑定的 1,800 例执行按主诊断分层五折 out-of-fold，任何病例及相同文本摘要均不进入自己的训练折；纯 Python 有界字符特征/倒排近邻不加载 Torch、sentence-transformers、PyArrow 或外部 Provider，也不写病例级 prediction/model/neighbor/error artifact。三次独立进程的 split 与 metrics 完全一致，权威 wrapper 结果为主诊断精确率 **89.56%**、全诊断 F1 **50.79%**、有 gold 主手术精确率 **66.38%**、全手术 F1 **83.80%**、完整集合精确匹配 **1.72%**；相对目录精确名称下界提升 80.50/28.30/50.27/67.53/1.72 个百分点。训练自身暴露、相同文本跨折泄漏和目录外代码均为 0，扩大回归 **223 passed、5 skipped**、静态预检 **111/111**。这证明本地监督开发基线和泄漏约束，不是外部 untouched cohort、独立临床 gold、Corti head-to-head、医院验收或生产质量；尤其完整集合精确匹配仍很低。CCL 资产和模型未进入 Runtime，用户本机授权不等于再分发/云/医院生产许可。

2026-08-27 的 .NET 最低版本兼容收敛见 [`ICODER_DOTNET_STANDARD20_COMPATIBILITY_PHASE_SUMMARY_2026-08-27.md`](ICODER_DOTNET_STANDARD20_COMPATIBILITY_PHASE_SUMMARY_2026-08-27.md)：按 Corti 官方 SDK Overview 明示的 `.NET Framework 4.6.2+` 与 `.NET Standard 2.0` 基线，iCoDer `beta.43` 新增 `netstandard2.0` 包资产并保留原生 `net8.0`/`net10.0`。最低 `netstandard2.0` 与 `net462` 消费者均 0 警告/0 错误，原生运行时合同各 **82/82**，实际 NuGet 精确包含三个目标资产；CI、发布候选和静态预检均失败关闭。该证据关闭源码/包/最低消费者的开发兼容差距，不代表已在真实 .NET Framework 应用运行、发布 NuGet、接通 Corti 托管租户或获得临床生产批准。

2026-08-27 的 CCL 2026 本地隔离预测评价门见 [`ICODER_CCL2026_LOCAL_PREDICTION_EVALUATOR_PHASE_SUMMARY_2026-08-27.md`](ICODER_CCL2026_LOCAL_PREDICTION_EVALUATOR_PHASE_SUMMARY_2026-08-27.md)：新增 1,800-case 逐例 digest/顺序/目录绑定与聚合计分合同，正式指标不折叠父子码或临床扩展码，安全失败进入分母，篡改、病例字段注入、目录外代码、外发声明和隔离目录越界均失败关闭且不产出可信指标。1,800/1,800 oracle 合同自检通过；真实的无网络纯目录下界为主诊断精确率 **9.06%**、全诊断 F1 **22.50%**、有主手术 gold 病例精确率 **16.12%**、全手术 F1 **16.27%**、完整集合精确匹配 **0%**。临时逐例包均已清除，聚合报告不含病例、病案号、代码字符串、逐例标签/预测或错误样本；扩大回归 **139 passed**、当时静态预检 **110/110**。只读运行时审计同时确认 BGE-M3 资产完整，但当前 Windows `torch 2.11.0 + sentence-transformers 3.2.1` 为已知访问冲突组合，且 BGE 仅是 embedding 检索器、没有获批准的本地生成式临床编码模型；故没有绕过安全门。oracle 与纯目录结果均明确 `model_capability_proven=false`；其“尚无本地模型质量测量”已由上方非生成式监督 OOF 开发基线部分替代，但独立 held-out 临床 gold、Corti 同病例输出、Runtime 部署许可和生产质量仍未完成。

2026-08-27 的 CCL 2026 本地数据与目录覆盖整改见 [`ICODER_CCL2026_LOCAL_DATASET_AND_RUNTIME_CATALOG_PHASE_SUMMARY_2026-08-27.md`](ICODER_CCL2026_LOCAL_DATASET_AND_RUNTIME_CATALOG_PHASE_SUMMARY_2026-08-27.md)：获授权 `train.xlsx` 的 1,800 条记录与仓内 fixture 按完整 case digest 逐条、顺序一致；聚合审计不输出病例正文、病案号或逐条标签，并继续固定 `external_provider_egress_allowed=false`。运行目录由 33,304/23,165 扩展为 **39,756 个诊断代码、28,394 个手术代码**，覆盖 CCL 的 960/960 唯一诊断码和 48/48 唯一手术码，9,442/2,172 次标签分配均为 0 未匹配。Runtime Matrix 对普通严格 26-Agent bundle 只派生结构确定的 23 个本地路径，optional-provider Agent 仍需专用本地 bundle；目录增量后的新鲜隔离 HTTP bundle 验证 happy/adversarial/reference 各 24/24、stability 144/144，并将本地矩阵记为 24/24。扩大回归 144 passed、5 skipped，预检 107/107。该结果提升中国编码版本覆盖与本地评测可追溯性，但没有外发许可、独立临床 gold、Corti 同病例输出或生产准确率结论。

2026-08-27 的双语编码独立 gold 工程门见 [`ICODER_BILINGUAL_CODING_GOLD_REVIEW_PHASE_SUMMARY_2026-08-27.md`](ICODER_BILINGUAL_CODING_GOLD_REVIEW_PHASE_SUMMARY_2026-08-27.md)：5-case reviewer packet 已彻底移除工程 expected、notes、旧 evidence 与模型输出；两名不同 reviewer 必须逐码提供目录成员和中英文精确 evidence，任何分歧强制第三名 adjudicator，最终还需外部核验身份、资质、签字、机构和利益冲突。真实临床校准 runner 与严格 wrapper 只在四份 artifact 全部通过时使用 adjudicated gold，并把输入复制进证据目录复验；否则在模型外发前失败关闭。扩大回归 **133 passed、5 skipped**，静态预检 **106/106**。当前只有 readiness bundle，没有外部 reviewer 提交，故 `independent_gold_ready=false`，HOBV1-001 父/子码仍待裁决，Corti 临床等价与生产状态不提升。

2026-08-27 的中国编码目录初始内置整改见 [`ICODER_CN_CODE_CATALOG_ASSET_PHASE_SUMMARY_2026-08-27.md`](ICODER_CN_CODE_CATALOG_ASSET_PHASE_SUMMARY_2026-08-27.md)：仓外相邻 checkout 依赖和两个示例码的静默 fallback 已移除，代码信任锚、资产清单、SHA-256、大小、记录数和 JSON 解析任一失败都会拒绝启动。其初始 33,304/23,165 数量已由上面的 CCL 覆盖增量更新为 39,756/28,394；本机仍未实际构建 Docker 镜像，且用户授权不等于公开再分发许可，真实镜像 build/scan、权威版本审批与目录许可仍为生产门。

2026-08-27 的修复后临床校准见 [`ICODER_CLINICAL_CALIBRATION_REMEDIATION_PHASE_SUMMARY_2026-08-27.md`](ICODER_CLINICAL_CALIBRATION_REMEDIATION_PHASE_SUMMARY_2026-08-27.md)：隔离真实 DeepSeek 串行完成 **50/50 fresh HTTP**，得到 **42 capability-passed + 8 safe-fail-closed**，但质量门仍未通过。8 次 CDI 失败已定位为内部脱敏遥测键污染严格公共合同并在本地修复；10 次 Medical Coding 输出发现 **13 个目录外缩写/幽灵码**，现已改为目录精确成员校验、撤下并强制人工复核。中英文目录候选覆盖本地提升到 **26/28（92.86%）**，聚焦回归 **129 passed、5 skipped**，静态预检 **104/104**。这些是修复后代码与本地检索证据，尚无修复后新鲜 50 次真实模型结果；不得表述为 Corti 临床质量等价或生产上线通过。

2026-08-26 的已登录 Corti Console 只读核对见 [`CORTI_CONSOLE_READ_ONLY_AGENT_AUDIT_2026-08-26.md`](CORTI_CONSOLE_READ_ONLY_AGENT_AUDIT_2026-08-26.md)：当前 AI Studio 明示 **20 个 Pre-built Agents**，iCoDer 对这 20 个名称均有 Hub 可见 Pack，并另有 6 个自有 Pack；CDI preset 进入可定制的 Agent 创建/预览路径，可见模板、定制、上下文与对话试用入口。本次未创建、运行或调用 Corti 能力，也未使用病例或消耗额度。该证据只把“当前可见目录映射”提升为 **20/20**，不提升行为、临床质量、工具链、时延、成本、生产 SLA 或医院上线等价；Triage 最终分级、权威外部来源、实际编码分配、登记库/Claims 集成、真实中国医疗数据和同病例盲评仍为开放差距。

Agent 模板与 Clone 运行单一真源收敛见 [`ICODER_AGENT_TEMPLATE_PACK_CLONE_PHASE_SUMMARY_2026-08-26.md`](ICODER_AGENT_TEMPLATE_PACK_CLONE_PHASE_SUMMARY_2026-08-26.md)：修复前模板目录为 22 项而 Hub 为 26 项，缺 12 个当前 ID、残留 6 个旧别名，并以手工提示词承诺超出真实 Runtime 的最终分诊、自动编码/登记与指南检索。当前模板接口精确投影 **26 个 Hub launch-candidate Pack + 2 个通用空白模板**，`missing/extra/stale_aliases` 均为 0；20 份旧医疗模板正文已移除。治理模板只能走幂等 Hub Clone，保留源 Runtime、权限、输出合同、非目标、Expert 图与审计身份；canonical Pack 下载、旧 REST Clone 失败关闭、前端分流和部署门禁均已覆盖。后端聚焦 **55/55**、可见 Agent 离线安全 **78/78**、前端全量 **144/144**、生产构建通过、预检 **102/102**。由此关闭的是 iCoDer 内部“模板目录/创建路径与 Hub Runtime 漂移”，不是 Corti 私有定制语义或临床行为等价。

Agent 生命周期与详情页真实性收敛见 [`ICODER_AGENT_LIFECYCLE_RUN_HISTORY_PHASE_SUMMARY_2026-08-26.md`](ICODER_AGENT_LIFECYCLE_RUN_HISTORY_PHASE_SUMMARY_2026-08-26.md)：项目 Agent 现以 `draft/published/archived` 后端状态机控制发布、归档、恢复、编辑、版本与删除；HTTP Run 和 A2A 均对未发布状态以 `agent_not_published` 失败关闭，全部生命周期动作写入内容受限的组织审计。AgentDetailPage 已移除本地伪造的金标准准确率/CSV 评估，改为后端持久化 Run History，并按后端生命周期禁用运行或编辑。新鲜文件级核验为 Corti 当前可见目录映射 **20/20**，Hub Runtime 为 executable/provider-resolvable/launch-candidate-ready **26/26**，但临床质量与生产就绪仍为 **0/20** 和 **0/26**。经历 PowerShell 兼容、CDI 跨事件循环连接池、本地 RuntimeError 和结构化输出不稳定的逐轮失败关闭与修复后，最终 `external_semantic_e2e_live_20260826-235457` 已从启动到清理一次通过：happy/adversarial/reference 各 **26/26**、stability **156/156**，全局 P50/P95 **0.348/3.751 秒**；CDI **6/6、P95 15.482/30 秒**，Medical Coding **6/6、P95 3.933/10 秒**。Bundle、Runtime Matrix、artifact validation 和顶层证据均有效，strict live semantic **26/26**，无 failure 文件，凭据扫描 0 命中，受保护数据库未变化。两条核心路径只允许一次结构化修复重试，仍不合规则失败关闭，不放宽 schema 或记录 Provider 原文；聚焦回归 **378/378**、静态预检 **104/104**。当前可登记“开发环境严格合成 Pack 的真实 Provider wrapper 已完整通过”，仍不能登记 Corti 私有生命周期/临床质量等价、独立临床金标准或生产上线；这些需 Corti head-to-head、临床 reviewer、医院验收与生产基础设施外部验证。

Transcripts 编码双声道与逐句毫秒时间戳收敛见 [`ICODER_TRANSCRIPTS_ENCODED_MULTICHANNEL_PHASE_SUMMARY_2026-08-25.md`](ICODER_TRANSCRIPTS_ENCODED_MULTICHANNEL_PHASE_SUMMARY_2026-08-25.md)：同步、持久异步和恢复路径现以受限 `ffprobe`/`ffmpeg` 隔离解码 Corti 公布的 WAV、Ogg、WebM、Opus、Vorbis、MP3、FLAC、M4A/AAC 合同，并继续要求 participants 精确覆盖 channel 0/1；有效 Provider 逐句段统一为整数毫秒。真实 Uvicorn + FFmpeg 的 PCM/FLAC loopback 在一次初始超时后连续完整通过 3 次；Ogg/Opus、WebM/Opus、Ogg/Vorbis、MP3、FLAC、MP4/AAC 的 10 MIME 同步+持久异步矩阵又以两个独立临时库完整通过 **20/20**，均证明合成频率声道隔离与逐句毫秒归属，但 ASR 边界仍为合成。最终后端四分片 **5,604 passed、20 skipped、11 deselected、0 failed**，加根级发布测试共 **5,617 passed**；JS **95/95**、Python **101/101**、.NET net8/net10 各 **82/82**、预检 **101/101**；三 SDK 升至 `beta.42/b42`，六个带哈希候选工件未发布。编码双声道接口、隔离解码合同和短合成音频逐格式 E2E 已关闭；diarization、词级时间戳、真实中国医疗音频质量、长音频/容量和 Corti head-to-head 仍开放。

Transcripts 双声道 PCM WAV 与 participant attribution 收敛见 [`ICODER_TRANSCRIPTS_MULTICHANNEL_PHASE_SUMMARY_2026-08-25.md`](ICODER_TRANSCRIPTS_MULTICHANNEL_PHASE_SUMMARY_2026-08-25.md)：`isMultichannel=true` 现要求 participants 精确覆盖 channel 0/1，并在同步、持久异步和恢复路径把时间对齐的 stereo 16 kHz/16-bit PCM WAV 有界拆为两个 mono 声道，分别转写后加密保存 channel/participant 结构。真实 Uvicorn loopback 用正负样本合成 WAV 证明无串扰和双路径归属，但 ASR 边界仍为合成。最终后端全量 **5,586 passed、20 skipped、11 deselected、0 failed**，JS **95/95**、Python **101/101**、.NET net8/net10 各 **82/82**、预检 **100/100**；三 SDK 升至 `beta.41/b41`，六个带哈希候选工件未发布。已关闭的是显式双声道 PCM WAV 合同，不包括编码/压缩多声道、diarization、逐句时间戳、真实中国医疗音频质量或 Corti head-to-head；这些历史缺口由后续 beta.42 阶段部分关闭或重新界定。

Transcripts keyterms 合同收敛见 [`ICODER_TRANSCRIPTS_KEYTERMS_PHASE_SUMMARY_2026-08-25.md`](ICODER_TRANSCRIPTS_KEYTERMS_PHASE_SUMMARY_2026-08-25.md)：预录音同步、异步 Job 和恢复路径现保持有序、大小写敏感的 `keyterms.terms[].term`，实施 1,000 项/每项 50 字符边界并映射到 FunASR `hotword`；请求数据加密持久化，日志与遥测不记录词项正文。真实 Uvicorn loopback 精确验证 `("房颤", "Corti Health")` 透传，但只使用合成 WAV/ASR 边界。最终后端全量 **5,575 passed、20 skipped、11 deselected、0 failed**，JS **95/95**、Python **101/101**、.NET net8/net10 各 **82/82**、预检 **98/98**；三 SDK 当时升至 `beta.40/b40`，六个带哈希候选工件未发布。预录音 keyterms 接口差距已关闭；当时开放的显式双声道 PCM WAV 差距已由后续 beta.41 阶段关闭，真实术语召回、多语言、自动标点控制、编码多声道、diarization 和 Corti head-to-head 仍开放。

Transcripts 中文口述标点与当前字段优先级收敛见 [`ICODER_TRANSCRIPTS_CHINESE_SPOKEN_PUNCTUATION_PHASE_SUMMARY_2026-08-25.md`](ICODER_TRANSCRIPTS_CHINESE_SPOKEN_PUNCTUATION_PHASE_SUMMARY_2026-08-25.md)：预录音同步、异步 Job 和恢复路径统一支持 `spokenPunctuation=true` 的 zh-* 显式口述标点；它优先于 `automaticPunctuation=false`，已弃用 `isDictation` 只在两个当前字段都缺失时兼容回退。真实 Uvicorn/租户鉴权/上传/轮询 loopback 通过，但 ASR 边界与 WAV 均为合成且未用真实 LLM/患者音频。最终后端全量 **5,574 passed、20 skipped、11 deselected、0 failed**，JS **95/95**、Python **101/101**、.NET net8/net10 各 **82/82**、预检 **97/97**；三 SDK 升至 `beta.39/b39`，六个带哈希候选工件未发布。预录音 keyterms、多声道、diarization、多语言、真实自动标点控制和临床音频 head-to-head 仍开放。

Streams keyterms 与当前字段合同收敛见 [`ICODER_STREAMS_KEYTERMS_CURRENT_CONTRACT_PHASE_SUMMARY_2026-08-25.md`](ICODER_STREAMS_KEYTERMS_CURRENT_CONTRACT_PHASE_SUMMARY_2026-08-25.md)：对齐 Corti 当前 `diarize` 主字段并继续兼容旧 `isDiarization` 输入；有序、大小写敏感的 keyterms 在 1,000 项/每项 50 字符边界内传入 FunASR `hotword`，日志和遥测不保存词项正文。最终后端全量 **5,568 passed、20 skipped、11 deselected、0 failed**，JS **95/95**、Python **101/101**、.NET net8/net10 各 **82/82**、预检 **95/95**；`beta.38/b38` 六个带哈希候选工件未发布。loopback 只证明配置、转发和安全失败合同，未启用真实 ASR/LLM/患者音频，故 diarization、keyterm 实际增益、中国医疗语音质量、同音频 Corti head-to-head 与生产容量仍开放。

Streams 多声道 PCM 与 `fast_init` 收敛见 [`ICODER_STREAMS_MULTICHANNEL_FAST_INIT_PHASE_SUMMARY_2026-08-25.md`](ICODER_STREAMS_MULTICHANNEL_FAST_INIT_PHASE_SUMMARY_2026-08-25.md)：当前接受 1–8 声道、16 kHz signed 16-bit little-endian 原始 PCM；多声道交错帧按声道拆分，participant 必须与 `0..channels-1` 精确一一对应，transcript/audioEvent/checkpoint 保留声道归属。Facts 支持 `fixed` 与近似 Corti 公布节奏的 `fast_init`。真实租户 loopback 覆盖三 SDK、malformed media、单声道 audio events 与双声道 fast_init；最终后端全量 **5,565 passed、20 skipped、11 deselected、0 failed**，JS **93/93**、Python **97/97**、.NET 双框架各 **78/78**、预检 **94/94**，`beta.37/b37` 候选未发布。测试只使用合成 PCM 且关闭真实 ASR/LLM；diarization、真实中国医疗语音质量、同音频 Corti head-to-head 与生产容量仍开放。

Streams 未结束交互恢复见 [`ICODER_STREAMS_CHECKPOINT_RECOVERY_PHASE_SUMMARY_2026-08-25.md`](ICODER_STREAMS_CHECKPOINT_RECOVERY_PHASE_SUMMARY_2026-08-25.md)：`057` 将 `retain` 会话的音频分块、transcript/Facts 进度和运行状态加密持久化，并以组织/主体/interaction、持久租约与 session UUID 三层 fencing；配置漂移、密文/摘要损坏、旧 Writer、缺加密能力和缺 checkpoint 均失败关闭。两个独立 Uvicorn 的强制崩溃接管恢复 640-byte 合成 PCM，正常结束后 recording 仅 1 份且 lease/checkpoint/chunk 均清零。后端相关回归 44/44 + 114/114、迁移/漂移 10/10、JS 91/91、Python 95/95、.NET 双框架各 77/77、离线 Agent 78/78、聚焦门禁 24/24、预检 93/93；`beta.36/b36` 五个候选工件未发布。Corti 公开文档允许重新打开 Streams WebSocket，但未公开 checkpoint 恢复计数与跨 Worker fencing，故该切片不能宣称已做 Corti 私有恢复语义等价验证。

CDI / Medical Coding 多病例校准门禁见 [`ICODER_CLINICAL_CALIBRATION_GATE_PHASE_SUMMARY_2026-08-25.md`](ICODER_CLINICAL_CALIBRATION_GATE_PHASE_SUMMARY_2026-08-25.md)：可安全外发的开发校准范围固定为 40 条声明脱敏 CDI 中文病例和 5 条 PHI-free 合成编码病例的中英双跑，共 50 次串行调用。CCL 1,800 条源工作簿与仓内 fixture 的技术来源绑定现已证明，但正式许可审查、独立脱敏证明和第三方处理授权仍缺失，因此 CCL 1,800/201/100 继续被代码门禁阻止外发，只允许本地隔离聚合评测。2026-08-27 已完整执行一次 50 次调用，但原报告未通过质量门；其后发现并修复当前输入溯源、双编码系统、证据锚定和种子标签治理问题，仍需新鲜 50 次复跑。不能据此声明准确率、Corti 等价或上线。

CDI / Medical Coding 严格真实 Provider 门禁的最新结果见 [`ICODER_AGENT_HUB_STRICT_LIVE_PROVIDER_E2E_PHASE_SUMMARY_2026-08-25.md`](ICODER_AGENT_HUB_STRICT_LIVE_PROVIDER_E2E_PHASE_SUMMARY_2026-08-25.md)：在隔离临时数据库和一次性 DeepSeek 凭据下，26 个 Hub 可见 Agent 的 happy/adversarial/reference 均为 **26/26**，三轮 stability 为 **156/156**；最终 bundle 有效，Runtime Matrix 的严格 live-provider 合成语义提升为 **26/26**。该证据只覆盖当前 Pack 自有合成病例的合同、安全、Trace、签名和重复性，不含独立临床金标准；50 次 CDI/Medical Coding 校准已执行但未通过质量门，Corti 等价、医院验收和生产就绪仍为 **0/26**。本文件后文及较早阶段段落中的 `0/26` 保留为当时快照，均由本段的最新严格语义口径取代，但不改变其生产与临床限制。

CDI 成本遥测收敛见已修订的 [`ICODER_CDI_COST_TELEMETRY_PHASE_SUMMARY_2026-08-25.md`](ICODER_CDI_COST_TELEMETRY_PHASE_SUMMARY_2026-08-25.md)：专用 CDI Runtime 只用 observed usage 和当前 CNY 配置价格生成 `configured_usage_pricing_estimate`，沿 A2A、统一 Run、RunHistory、Usage 和内容无关 Trace 传播，并固定 `billing_authoritative=false`。旧 Trace 的 CNY 0.00428106 仅是主阶段/Expert 历史下界；新 `224918` 源级 stability 已覆盖逐 Query 安全门，CDI 6/6 成本已知、调用数 7–11、配置估算合计 CNY 0.01006110，总覆盖 156/156。由于 wrapper 顶层终态失败，这不改写原 `212957` 权威报告的 150/156；Provider 发票对账、顶层 phase success 和独立临床/生产门禁仍开放。

CDI 安全门计量与时延阶段见更新后的 [`ICODER_CDI_GATE_TELEMETRY_LATENCY_PHASE_SUMMARY_2026-08-25.md`](ICODER_CDI_GATE_TELEMETRY_LATENCY_PHASE_SUMMARY_2026-08-25.md)：两个逐 Query 必需安全门现已进入内容无关 provider/model/token/latency 聚合，11 个阶段具有内部耗时归因；同一安全门内独立调用以最大 2 路并发并保持顺序，主临床因果链不变。fresh 源报告为 happy/adversarial/reference 各 **26/26**、stability **156/156**、成本覆盖 **156/156**；CDI P50/P95 **20.378/26.662 秒**、6/6 成本已知，Medical Coding **5.511/6.688 秒**。但 wrapper 在有效 bundle/Runtime Matrix 后、顶层 evidence 前终止，forensic recovery 明确不提升为顶层权威成功；并发时延误减字段随后已修正，最终无网络扩大回归 **493/493**、预检 **101/101**。30/10 秒均为开发预算，不是 Corti SLA；独立临床与生产门禁不变。

Triage 最新收敛见 [`ICODER_GOVERNED_TRIAGE_QUESTIONNAIRE_PHASE_SUMMARY_2026-08-25.md`](ICODER_GOVERNED_TRIAGE_QUESTIONNAIRE_PHASE_SUMMARY_2026-08-25.md)：`triage` 已改为 `icoder.governed-triage-questionnaire.v1`，只验证调用方提供的有界问卷定义、显式结构化答案和唯一来源 span，沿确定性分支生成开发期协议候选；不从对话抽取或推断答案，不做临床推理、医学计算、外部知识调用或最终 acuity 分配，不触发动作或写回。最新重签 loopback HTTP 为 happy/adversarial/reference 各 24/24、stability 144/144，P50 0.527 秒、P95 1.025 秒；离线安全 78/78、字段关系 340/340、证据绑定 60/60、跨 Agent 关系 20/20、静态预检 91/91。当前分类为 2 个外部 LLM 必需、1 个可选、23 个纯本地、24 个离线本地基线；严格 26-Agent live-provider 和生产验证仍为 0/26。Corti 当前公开 Triage 能力中的对话字段抽取、交互访谈、PubMed/Medical Calculator/DrugBank 工具、真实医院规则和经验证最终分诊级别仍未复刻。

Principal Diagnosis Review 最新收敛见 [`ICODER_GOVERNED_PRINCIPAL_DIAGNOSIS_REVIEW_PHASE_SUMMARY_2026-08-24.md`](ICODER_GOVERNED_PRINCIPAL_DIAGNOSIS_REVIEW_PHASE_SUMMARY_2026-08-24.md)：`principal-diagnosis-review` 已从会自动推荐主诊断且含零 span 的 pure-LLM 模板改为 `icoder.governed-principal-diagnosis-review.v1`，只复核编码员明确提供的初稿、候选集合、声明依据和精确 evidence span；不提取、分配、排序、推荐或替换诊断，不应用外部编码/病案首页规则，不提交或写回。最终签名 loopback HTTP 为 happy/adversarial/reference 各 22/22、stability 132/132，宽回归 1203 passed、5 skipped、0 failed，字段关系 301/301、证据绑定 56/56、静态预检 90/90。当前分类为 4 个外部 LLM 必需、1 个可选、21 个纯本地、22 个离线本地基线；严格 26-Agent live-provider 和生产验证仍为 0/26。Corti 没有公开同名独立 Agent；相邻 Medical Coding Agent/API 的自由文本诊断/操作抽取、主/次诊断与程序编码分配、顺序/modifier 验证、alternatives 和完整编码规则工具链仍未复刻。

Clinical Guidelines 最新收敛见 [`ICODER_GOVERNED_CLINICAL_GUIDELINES_PHASE_SUMMARY_2026-08-24.md`](ICODER_GOVERNED_CLINICAL_GUIDELINES_PHASE_SUMMARY_2026-08-24.md)：`clinical-guidelines` 已改为 `icoder.governed-clinical-guidelines.v1`，只对用户明确提供的医院批准来源元数据、指南条款、确定性 PRESENT/EQUALS/TIME_WINDOW_HOURS 规则与病例事实执行比较，支持重复字段冲突识别和精确 evidence span；不联网检索、不验证来源真实性/许可/最新版本、不做临床适用性/意义或治疗推理，不写回且始终人工复核。最终签名 loopback HTTP 为 happy/adversarial/reference 各 21/21、stability 126/126，宽回归 1193 passed、5 skipped、0 failed，字段关系 287/287、证据绑定 52/52、静态预检 90/90。当前分类为 5 个外部 LLM 必需、1 个可选、20 个纯本地、21 个离线本地基线；严格 26-Agent live-provider 和生产验证仍为 0/26。Corti 公开 Clinical Guidelines Agent 的域限制 Web Search/Web Fetch、来源真实性与版本时效验证、多文档自由文本综合、适用性、推荐强度/临床意义和章节级引用仍未复刻。

Clinical Education 最新收敛见 [`ICODER_GOVERNED_CLINICAL_EDUCATION_PHASE_SUMMARY_2026-08-24.md`](ICODER_GOVERNED_CLINICAL_EDUCATION_PHASE_SUMMARY_2026-08-24.md)：`clinical-education` 已改为 `icoder.governed-clinical-education.v1`，只从明确批准、带版本与范围的医院来源原句装配学习目标、要点和开放式复习题，并绑定精确 evidence span；不做问题分类、诊断/治疗/药物相互作用推理、外部检索、计算器、学习者自适应或患者建议，不发布或写回。最终签名 loopback HTTP 为 happy/adversarial/reference 各 20/20、stability 120/120，宽回归 1176 passed、5 skipped、0 failed，字段关系 262/262、证据绑定 50/50、静态预检 90/90。当前分类为 6 个外部 LLM 必需、1 个可选、19 个纯本地、20 个离线本地基线；严格 26-Agent live-provider 和生产验证仍为 0/26。Corti 公开 Clinical Education Agent 的问题分类、Quick/Tutor/Board-style、临床教学推理、PubMed/Web/Medical Calculator 与学习者适配仍未复刻。

Denial Appeals 最新收敛见 [`ICODER_GOVERNED_DENIAL_APPEALS_PHASE_SUMMARY_2026-08-24.md`](ICODER_GOVERNED_DENIAL_APPEALS_PHASE_SUMMARY_2026-08-24.md)：`denial-appeals` 已改为 `icoder.governed-denial-appeals.v1`，只从明确标注的拒付、结算、病历、授权、待遇资格和用户提供政策字段装配可追溯的 review-only 申诉草案或更正清单；不自动分类、推断根因、选择路径、验证编码/医疗必要性/待遇资格或检索支付政策，不提交或写回。最终签名 loopback HTTP 为 happy/adversarial/reference 各 19/19、stability 114/114，宽回归 1167 passed、5 skipped、0 failed，字段关系 234/234、证据绑定 48/48、静态预检 90/90。当前分类为 7 个外部 LLM 必需、1 个可选、18 个纯本地、19 个离线本地基线；严格 26-Agent live-provider 和生产验证仍为 0/26。Corti 公开 Denial Appeals Agent 的自动分类/路径、编码与 modifier/units 校验、PubMed/Web/Medical Coding 专家、payer-ready 工作流和结果分析仍未复刻。

Claim Check 最新收敛见 [`ICODER_GOVERNED_CLAIM_CHECK_PHASE_SUMMARY_2026-08-24.md`](ICODER_GOVERNED_CLAIM_CHECK_PHASE_SUMMARY_2026-08-24.md)：`claim-check` 已改为 `icoder.governed-claim-check.v1`，只把明确结算、病历和用户提供的版本化政策事实装配成 review-only 核查包并绑定脱敏输入精确 span；固定不判断编码支持、医疗必要性、待遇资格、支付/拒付、HCC/DRG/DIP，不提交或写回。最终签名 loopback HTTP 为 happy/adversarial/reference 各 18/18、stability 108/108，宽回归 1156 passed、5 skipped、0 failed，字段关系 216/216、证据绑定 46/46、静态预检 90/90。当前分类为 8 个外部 LLM 必需、1 个可选、17 个纯本地、18 个离线本地基线；严格 26-Agent live-provider 和生产验证仍为 0/26。Corti 当前公开资料没有同名独立 Agent，邻近对照为 Revenue Cycle 的文档支持编码、原申报比较、evidence、HCC/DRG 和 Claims 集成，后四类核心能力仍未复刻。

Referral Generator 最新收敛见 [`ICODER_GOVERNED_REFERRAL_GENERATOR_PHASE_SUMMARY_2026-08-24.md`](ICODER_GOVERNED_REFERRAL_GENERATOR_PHASE_SUMMARY_2026-08-24.md)：`referral-gen` 已改为 `icoder.governed-referral.v1`，只从明确标题字段装配可追溯转诊信；核心字段缺失即不出信，固定禁止临床推断、新诊断/治疗、自动发送和写回。真实本地 HTTP 为 happy/adversarial/reference 各 16/16、stability 96/96，宽回归 1108 passed、5 skipped、0 failed，字段关系 184/184、证据绑定 42/42、静态预检 90/90。最新分类为 10 个外部 LLM 必需、1 个可选、15 个纯本地、16 个离线本地基线；严格语义和生产验证仍为 0/26。自由叙事、多文档综合、专科模板、区域转诊闭环和独立临床质量仍开放。

Agent Hub Clone 与预置投影一致性收敛见 [`ICODER_AGENT_HUB_CLONE_PROJECTION_PHASE_SUMMARY_2026-08-23.md`](ICODER_AGENT_HUB_CLONE_PROJECTION_PHASE_SUMMARY_2026-08-23.md)，其后续项目运行闭环见 [`ICODER_AGENT_HUB_PROJECT_CLONE_RUNTIME_PHASE_SUMMARY_2026-08-23.md`](ICODER_AGENT_HUB_PROJECT_CLONE_RUNTIME_PHASE_SUMMARY_2026-08-23.md)：Hub 与 Clone 使用同一可见 Pack 权威边界，隐藏/metadata/stub/internal Pack 失败关闭；Registry→DB 完整投影、字段漂移修复和 `registry_projection_managed` ownership 关闭了 Clone 404 与重复启动漂移。后续实现已经让 `project_agent_id` 沿源 Provider 或 Medical Coding/CDI 专用 Runtime 执行，安全应用项目 prompt、已发布租户 Expert 和 Connector Graph，同时保持源 Pack、源 Expert 安全图、合同和权限不可变；Run/A2A/Trace/Audit 均保留项目身份与源运行身份。因此早期“Clone 定制执行仍为 P0”的结论已经关闭，不再作为当前开放项。

Agent Hub 真实语义证据门禁见 [`ICODER_AGENT_HUB_SEMANTIC_EVIDENCE_GATE_PHASE_SUMMARY_2026-08-23.md`](ICODER_AGENT_HUB_SEMANTIC_EVIDENCE_GATE_PHASE_SUMMARY_2026-08-23.md)：示例、对抗、参考回放和稳定性报告现在必须同时通过 Pack/schema 快照、响应/Trace 文件哈希、HMAC 结果与 Trace attestation、Provider/model/usage 遥测、时效及种子来源复核；mock、degraded、fallback、resume、陈旧、缺失或伪造证据均失败关闭。历史报告被 140 项原因拒绝且保持 0/26；当前未提供新鲜真实 Provider bundle，因此语义验证与生产验证仍为 0/26。调整 Windows 全量负载的有界等待后，最新单体后端回归为 **5254 passed、20 skipped、11 deselected、0 failed**，语义/Trace 聚焦回归 **68/68**，静态部署预检 **82/82**。

较早的 7-Agent 本地语义 HTTP E2E 收敛见 [`ICODER_AGENT_HUB_LOCAL_SEMANTIC_E2E_PHASE_SUMMARY_2026-08-24.md`](ICODER_AGENT_HUB_LOCAL_SEMANTIC_E2E_PHASE_SUMMARY_2026-08-24.md)：该阶段建立了与严格 26-Agent 门禁隔离的受限证据，并完成 happy/adversarial/reference **7/7** 与 stability **42/42**。其数量口径已被下面的 Procedure Extractor 阶段更新，但 Navigator 中文术语边界、Note Completeness 显式节段冲突规则和“局部证据不得冒充完整门禁”的结论继续有效。

Procedure Extractor 受治理本地基线见 [`ICODER_GOVERNED_PROCEDURE_EXTRACTOR_PHASE_SUMMARY_2026-08-24.md`](ICODER_GOVERNED_PROCEDURE_EXTRACTOR_PHASE_SUMMARY_2026-08-24.md)：`procedure-extractor` 已从强制 PureLLM 改为 `icoder.governed-procedure-extractor.v1`，只提取带逐字 span 的明示术式和 performed/planned/historical/cancelled/negated/unknown 状态；仅在固定哈希 ICD-9-CM-3 目录得到唯一词法命中时输出候选码。E2E 同时发现原 Pack 将 T12 骨折内固定错误映射为 `81.0100 — 寰-枢脊柱融合` 且 span 错位，现修为目录中的 `03.5304 — 胸椎骨折切开复位内固定术` 和 `[22,38]`。该阶段真实临时租户 HTTP 为 happy/adversarial/reference 各 **8/8**、三轮 stability **48/48**（全部 fresh HTTP）、本地 bundle **8/8**；当时外部模型强依赖降至 **18**。该数量口径现已被 Diagnosis Extractor 的 9/17 阶段更新，但 Procedure 的语义和缺陷修复证据继续有效。

Evidence Extractor 受治理精确提及定位收敛见 [`ICODER_GOVERNED_EVIDENCE_EXTRACTOR_PHASE_SUMMARY_2026-08-23.md`](ICODER_GOVERNED_EVIDENCE_EXTRACTOR_PHASE_SUMMARY_2026-08-23.md)：统一 Run、A2A、Hub readiness、Trace 与 Pack 已统一到 `icoder.governed-evidence-extractor.v1`，只对显式候选编码定位固定 ICD-10-CN 目录术语或完整代码字面量的精确 span，并显式区分否定、既往、家族史和疑似上下文；它不判断临床支持、诊断成立或编码有效性，也不生成新编码。当前离线矩阵为 **6 个本地基线成功 + 20 个模型依赖安全失败关闭**，干净扩展回归 **915 passed、5 skipped、6 deselected、0 failed**，离线双场景 **52/52**，静态预检 **81/81**；Corti 邻近 Medical Coding 公开的全病历证据抽取、模型推理和 per-code supporting evidence 仍未复刻。

Evidence Ranker 受治理文档可追溯性收敛见 [`ICODER_GOVERNED_EVIDENCE_RANKER_PHASE_SUMMARY_2026-08-23.md`](ICODER_GOVERNED_EVIDENCE_RANKER_PHASE_SUMMARY_2026-08-23.md)：统一 Run、A2A、Hub readiness、trace 与 Pack 已统一到 `icoder.governed-evidence-ranker.v1`，按显式来源、字符 span、否定/不确定标记和候选码词面出现执行可解释排序；分数固定声明不是临床证据强度、诊断概率或编码置信度。当前 26-Agent 离线矩阵为 **5 个本地基线成功 + 21 个模型依赖安全失败关闭**，最终扩大回归 **986/986**、离线双场景 **52/52**、字段关系对抗 **63/63**、静态预检 **81/81**；Corti 当前公开 Library 无同名独立 Agent，邻近 Medical Coding 的模型推理/证据抽取能力仍未复刻。

较早的 ICD-10 Navigator 受治理本地索引收敛见 [`ICODER_GOVERNED_ICD_NAVIGATOR_PHASE_SUMMARY_2026-08-23.md`](ICODER_GOVERNED_ICD_NAVIGATOR_PHASE_SUMMARY_2026-08-23.md)：统一 Run、A2A、Hub readiness、trace 与 Pack 已统一到 `icoder.governed-icd-navigator.v1`，以哈希固定的 ICD-10-CN 目录和 56,424 条术语反向索引执行 Search、最多一次明确复合词拆分及一层 Explore；不执行 Verify/Guidelines，不分配或推荐编码。当时的 4/22 离线矩阵和 967/967 是 Evidence Ranker 收敛前的历史快照；目录来源/许可、权威说明、临床质量和生产审批仍未通过。

Code Validation 受治理本地目录收敛见 [`ICODER_GOVERNED_CODE_VALIDATION_PHASE_SUMMARY_2026-08-23.md`](ICODER_GOVERNED_CODE_VALIDATION_PHASE_SUMMARY_2026-08-23.md)：统一 Run、A2A、Hub readiness 与 Pack 已统一到 `icoder.governed-code-validation.v1`，默认以哈希固定的 ICD-10-CN / ICD-9-CM-3 目录执行成员和可分配性基线，可选 LLM 不得覆盖目录事实。两份目录仍为来源未核验、许可待外部复核、非结算权威，因此全部结果强制人工复核且 `production_ready=false`。26-Agent 离线矩阵现为 **3 个本地基线成功 + 23 个模型依赖安全失败关闭**，本阶段扩展回归 **957 passed、5 skipped、0 failed**；静态语义质量与生产审批仍为 0/26。

较早的 Note Completeness 本地确定性收敛见 [`ICODER_NOTE_COMPLETENESS_LOCAL_RULES_PHASE_SUMMARY_2026-08-23.md`](ICODER_NOTE_COMPLETENESS_LOCAL_RULES_PHASE_SUMMARY_2026-08-23.md)：其中 **2 个本地能力 + 24 个模型依赖** 和 **602/602** 是 Code Validation 本地基线落地前的历史快照；章节级结构完整性能力边界仍有效。

Agent Hub 租户运行就绪复核见 [`ICODER_AGENT_HUB_TENANT_RUNTIME_READINESS_PHASE_SUMMARY_2026-08-23.md`](ICODER_AGENT_HUB_TENANT_RUNTIME_READINESS_PHASE_SUMMARY_2026-08-23.md)：公开 Hub 现在对 26/26 卡片统一保持 `not_checked` 和操作禁用，只有 Bearer 鉴权后的 schema `1.0` 就绪接口才返回当前组织的模型选择、配置和默认 900 秒有效的连通性证据。探针/Canary 按组织与部署隔离，切换部署、证据过期和明确失败均有接口级验证；前端对缺失、重复、目标不一致或矛盾项整批失败关闭。后端扩大回归 **127/127**、离线 Agent E2E **52/52**、前端 **141/141** 与生产构建、JavaScript **52/52**、Python **53/53**、OpenAPI 与预检 **81/81** 通过；.NET 仍因本机无 CLI 只记源码/CI 合同。

较早的 Agent Hub 运行就绪真实性复核见 [`ICODER_AGENT_HUB_RUNTIME_READINESS_PHASE_SUMMARY_2026-08-23.md`](ICODER_AGENT_HUB_RUNTIME_READINESS_PHASE_SUMMARY_2026-08-23.md)：schema `1.3` 已将结构、配置、语义验证和生产审批分轴。其“公开 Hub 直接显示本地 ready/模型 unavailable”的投影已被更严格的公开/租户边界替代；其中 23 个外部 LLM 必需、1 个可选、2 个纯本地和 3 个本地基线是 Navigator 收敛前的历史口径；live health/语义验证/生产审批不会被虚假提升。

SDK 运行就绪合同复核见 [`ICODER_AGENT_HUB_SDK_RUNTIME_READINESS_PHASE_SUMMARY_2026-08-23.md`](ICODER_AGENT_HUB_SDK_RUNTIME_READINESS_PHASE_SUMMARY_2026-08-23.md)：其 schema `1.3` 四轴类型、不可用操作失败关闭和 A2A v0.3 发现卡分型继续有效；当前版本已升级为 JavaScript/.NET `beta.44` 与 Python `b44`。.NET 包现覆盖 `netstandard2.0`/`net8.0`/`net10.0`，并以 `net462` 最低消费者编译门禁对齐 Corti 公布基线；三语言 Models 资源新增临床模型包治理面，Transcripts 继续覆盖当前口述标点、有界 keyterms、显式 stereo PCM WAV participant 0/1、Corti 公布的编码容器 MIME 集和双声道失败关闭，同时保留鉴权就绪读取、schema `1.0` 矛盾状态拒绝、Streams checkpoint resume、多声道 PCM、`fast_init`、当前 `diarize` 与有界 keyterms。

SDK 运行时韧性复核见 [`ICODER_SDK_RUNTIME_RESILIENCE_PHASE_SUMMARY_2026-08-23.md`](ICODER_SDK_RUNTIME_RESILIENCE_PHASE_SUMMARY_2026-08-23.md)：按 Corti 官方 SDK 的自动 token refresh、并发安全刷新和可配置 429/5xx retry 基线，JavaScript/Python 已补齐自动 client-credentials、single-flight/线程锁刷新、一次 401 重放、`Retry-After` 封顶和幂等感知重试；鉴权异常脱敏分型。该阶段 JS **58/58**、Python **59/59**；其当时开放的 typed errors、pagination 与 managed WebSocket 已在后续阶段收敛，.NET 本机实跑和 Corti 托管租户互操作仍开放。

SDK 类型化错误与自动分页见 [`ICODER_SDK_TYPED_ERROR_PAGINATION_PHASE_SUMMARY_2026-08-23.md`](ICODER_SDK_TYPED_ERROR_PAGINATION_PHASE_SUMMARY_2026-08-23.md)：Corti 官方精确重试范围包含 408、429、5xx，并公开九类状态异常及 async pager。iCoDer JavaScript/Python 已补齐 408、统一 PHI-safe API 错误、A2A 稳定 reason 兼容，以及 Task/Context/Context Task/Trace 的惰性 cursor 迭代与循环/页数门禁；该阶段 JS **63/63**、Python **64/64**。旧 page/limit 资源统一、请求级选项、.NET 与托管互操作仍开放；managed WebSocket 已由下一阶段关闭开发环境差距。

SDK 托管实时 STT 与音频外发安全见 [`ICODER_SDK_MANAGED_STT_EGRESS_SAFETY_PHASE_SUMMARY_2026-08-23.md`](ICODER_SDK_MANAGED_STT_EGRESS_SAFETY_PHASE_SUMMARY_2026-08-23.md)：该文档记录了当时只允许 pre-audio 重连的历史边界；默认禁止隐式公共 Provider、原生模型加载和自由文本错误泄漏的安全结论继续有效。

实时 STT 断线恢复收敛见 [`ICODER_REALTIME_STT_RESUME_PHASE_SUMMARY_2026-08-24.md`](ICODER_REALTIME_STT_RESUME_PHASE_SUMMARY_2026-08-24.md)：服务端与 JavaScript/Python/.NET `beta.30` 协商 `icoder.stt-resume.v1`，以序号帧、ACK、连接内去重、完整 end cursor 和最多 32 MiB 的客户端内存重放关闭 post-audio 恢复缺口；无效 ACK 会终止连接，旧服务端继续失败关闭。两个独立 Uvicorn 经 round-robin 故障代理完成 **3 个 SDK 会话、3 次强制断线、6 次总连接、3 次跨进程恢复**；后端 **20/20**、JS **78/78**、Python **82/82**、.NET net8/net10 各 **65/65**，静态部署预检 **84/84**。该证据不使用真实音频或 Provider，不能提升真实转写质量。

Streams 初始合同见 [`ICODER_STREAMS_PHASE_SUMMARY_2026-08-24.md`](ICODER_STREAMS_PHASE_SUMMARY_2026-08-24.md)，其“multichannel、audio events、fast_init、session keyterms 均失败关闭”是历史边界：audio events 已在后续 PCM 阶段关闭，多声道 PCM 与 `fast_init` 已在 2026-08-25 阶段关闭，session keyterms 又在当前字段合同阶段关闭。当前仍失败关闭的是 diarization 和未声明/不安全的媒体组合；真实 STT/LLM 临床质量仍未证明。

Streams 跨 Worker 协调收敛见 [`ICODER_STREAMS_MULTIWORKER_LEASE_PHASE_SUMMARY_2026-08-24.md`](ICODER_STREAMS_MULTIWORKER_LEASE_PHASE_SUMMARY_2026-08-24.md)：`056` 增加按组织/主体/interaction 复合主键的持久租约，session UUID 作为 fencing token；原子抢占、6–300 秒 TTL、心跳、flush/end 前确认和精确释放防止旧 Worker 续租或删除后继会话。两个独立 Uvicorn 共享临时 SQLite WAL 时，第二 Worker 拒绝活跃冲突；主 Worker 被强制终止后，第二 Worker 在过期后用新 fence 接管并完成，最终租约 0。扩大回归 **69/69**、迁移/漂移 **10/10**、部署预检 **86/86**；三 SDK 单 Worker E2E 继续通过。

Streams 编码音频真实性收敛见 [`ICODER_STREAM_AUDIO_FORMAT_PHASE_SUMMARY_2026-08-24.md`](ICODER_STREAM_AUDIO_FORMAT_PHASE_SUMMARY_2026-08-24.md)：服务端不再接受 WAV/raw 或把任意字节默认为 WebM；配置声明、首 512-byte 容器探测、codec 与实际 MIME 一致性在 ASR/留存前失败关闭，三 SDK 同步预传输拒绝。E2E 改用生成的静音 Ogg/Opus，并发现及修复 `flush` 后 `end` 重复调用同一音频的成本风险。当前后端扩大回归 **54/54**、JS **87/87**、Python **91/91**、.NET net8/net10 各 **72/72**、迁移/漂移扩大矩阵 **19/19**、部署预检 **87/87**；`beta.32` 四个包已生成哈希但未发布。

Streams 隔离媒体解码收敛见 [`ICODER_STREAM_MEDIA_DECODER_PHASE_SUMMARY_2026-08-24.md`](ICODER_STREAM_MEDIA_DECODER_PHASE_SUMMARY_2026-08-24.md)：通过头门的媒体必须在固定 ffmpeg 子进程内以 stdin、无 stdout/stderr、单线程、1 MiB probe、1 frame、64 MiB 单次分配和默认 3 秒超时完成解码，才允许 ASR/留存；云模式禁止 `header_only`。三 SDK 合法静音 Ogg/Opus 与伪 `OggS/OpusHead` 负向真实 WebSocket 均通过，负向会话未触达 ASR/留存。后端联合矩阵 **119/119**、JS **87/87**、Python **91/91**、.NET 双框架各 **72/72**、迁移/漂移 **19/19**、预检 **88/88**；`beta.33` 候选未发布。Docker CLI 不可用，镜像构建/扫描仍开放。

Streams 解码并发与格式/变异 soak 收敛见 [`ICODER_STREAM_MEDIA_SOAK_PHASE_SUMMARY_2026-08-24.md`](ICODER_STREAM_MEDIA_SOAK_PHASE_SUMMARY_2026-08-24.md)：解码器现在具有进程级有界并发、排队超时、`AUDIO_VALIDATION_BUSY` 失败关闭、取消时 kill/reap 和无内容健康计数。真实 ffmpeg 生成 Ogg/Opus、WebM/Opus、MP3、FLAC、MP4/AAC 五种静音容器，并以固定种子执行 5 个可疑伪容器和 90 个确定性变异，共 **100/100** 完成；69 个可解码、31 个安全拒绝，最大并发 4，busy/timeout/unavailable/cancelled 与残留进程均为 0。后端联合矩阵 **137/137**、JS **87/87**、Python **91/91**、.NET 双框架各 **72/72**、迁移/漂移 **19/19**、发布验证器 **5/5**、预检 **88/88**；`beta.34` 四个候选工件未发布。该 soak 是合成静音与确定性变异，不是长期 fuzz、真实患者音频或 Linux 容器安全证明。

Streams 推荐 PCM 与音频健康事件收敛见 [`ICODER_STREAM_PCM_AUDIO_EVENTS_PHASE_SUMMARY_2026-08-24.md`](ICODER_STREAM_PCM_AUDIO_EVENTS_PHASE_SUMMARY_2026-08-24.md)：对齐 Corti 当前公开的 `audio/pcm` 参数合同，并把 16 kHz、单声道、16-bit、little-endian、signed 推荐配置作为唯一可上线候选 profile。PCM 完整帧、固定 ffmpeg 输入参数、WAV 适配、本地 STT 关闭时的显式 `STT_UNAVAILABLE`、四类类型化 audioEvent 和无内容审计均已覆盖。真实租户 WebSocket 以合成 PCM 依次产生 silence detected/recovered 与 quality issue detected/recovered，并经 decoder→STT adapter→usage→ENDED 完成；单/双 Worker E2E 通过。后端联合矩阵 **255/255**、JS **89/89**、Python **93/93**、.NET 双框架各 **74/74**、迁移/漂移 **19/19**、发布验证器 **5/5**、预检 **89/89**；`beta.35` 四个候选工件未发布。其他 Corti 合法 PCM profile、编码容器上的 audio events、真实中文医疗 ASR 与临床事件质量仍未关闭。

Streams 加密断点恢复收敛见 [`ICODER_STREAMS_CHECKPOINT_RECOVERY_PHASE_SUMMARY_2026-08-25.md`](ICODER_STREAMS_CHECKPOINT_RECOVERY_PHASE_SUMMARY_2026-08-25.md)：`retain` 会话现在逐块事务化保存加密音频与 transcript/Facts/usage 状态，异常退出后按新 session fence 恢复，三 SDK 只在服务端明确 `resumed=true` 时接受恢复，并只把已收到 `flushed` 回执的音频自动判为安全可恢复。配置漂移、tamper、旧 Writer 和无加密均失败关闭；双 Worker 强制终止 E2E 通过。首次全量后端回归暴露的 22 项旧契约/夹具与 MCP 空错误问题已逐项整改，最终 **5,559 passed、20 skipped、11 deselected、0 failed**；当前 JS **91/91**、Python **95/95**、.NET 双框架各 **77/77**、迁移/漂移 **10/10**、STT/Facts API **114/114**、发布验证器 **5/5**、预检 **93/93**；`beta.36/b36` 五个候选工件未发布。真实 ASR/Facts、生产 PostgreSQL 多副本与 Corti 私有重连状态仍开放。

SDK 请求控制、真实路由与发布物收敛见 [`ICODER_SDK_REQUEST_CONTROLS_ROUTE_TRUTHFULNESS_PHASE_SUMMARY_2026-08-23.md`](ICODER_SDK_REQUEST_CONTROLS_ROUTE_TRUTHFULNESS_PHASE_SUMMARY_2026-08-23.md)：JavaScript/Python 普通 REST、A2A、Run SSE 与单次 Artifact 下载均已加入有界逐请求 timeout/retry/cancellation/headers/query；签名 token、分页字段、协议头、身份/租户头和同源边界失败关闭。Billing 改为真实 page-number 分页，Expert SDK 对齐只读 v1 路由，Reviews/Marketplace/punctuate 幽灵 facade 及其发布物残留清零。最终 JS **74/74**、Python **75/75**、后端 **5263 passed、20 skipped、11 deselected、0 failed**、部署预检 **83/83**；npm 54 files、wheel 27 files，旧资源成员均为 0。

跨语言请求控制与真实 E2E 收敛见 [`ICODER_CROSS_SDK_REQUEST_PARITY_PHASE_SUMMARY_2026-08-24.md`](ICODER_CROSS_SDK_REQUEST_PARITY_PHASE_SUMMARY_2026-08-24.md)：Python 已补齐 Compliance/Runtime/Patient Context；.NET 所有公开 HTTP 方法已具备同源、身份/租户/协议字段失败关闭的 timeout/retry/cancellation/headers/query 控制，并在本机 net8.0/net10.0 各 **61/61**。三语言经单进程及双 API 进程 round-robin SSE 真实 HTTP E2E 全部通过，验证 26/26 Hub、确定性本地 Note Completeness、OAuth machine delegation、Run/Trace/SSE/Context/Recording/STT；JS **74/74**、Python **78/78**、路由/配置/预检专项 **54/54**、发布校验 **5/5**、部署预检 **83/83**。四个 `beta.29` 候选包已生成哈希但未发布。

较早的 26-Agent 执行真实性复核见 [`ICODER_AGENT_HUB_EXECUTION_TRUTH_PHASE_SUMMARY_2026-08-23.md`](ICODER_AGENT_HUB_EXECUTION_TRUTH_PHASE_SUMMARY_2026-08-23.md)：运行矩阵 v3 继续把“Provider 类可解析”与“真实语义已验证”分开；其中 25/1、23/1/2/3、22/1/3/4、20/1/5/6、Surgical Registry 后的 19/1/6/7、Procedure Extractor 后的 18/1/7/8，以及 Diagnosis Extractor 后的 17/1/8/9 均为历史基线；最新分类见后续 Rule Explainer 段。严格真实语义验证仍为 0/26、生产就绪验证仍为 0/26。

Diagnosis Extractor 本地收敛见 [`ICODER_GOVERNED_DIAGNOSIS_EXTRACTOR_PHASE_SUMMARY_2026-08-24.md`](ICODER_GOVERNED_DIAGNOSIS_EXTRACTOR_PHASE_SUMMARY_2026-08-24.md)：当前诊断只来自明示诊断标签，疑似/否定/既往/家族史隔离，逐字 span、唯一 ICD-10-CN 目录候选和 `unresolved` 失败关闭经 Provider、Run/A2A/Trace 验证。冻结 v6 未被改写，新增语义以追加式 v7 注册；真实 loopback HTTP 为 happy/adversarial/reference 各 9/9、stability 54/54，扩大回归 337/337、静态预检 90/90。

Rule Explainer 本地收敛见 [`ICODER_GOVERNED_RULE_EXPLAINER_PHASE_SUMMARY_2026-08-24.md`](ICODER_GOVERNED_RULE_EXPLAINER_PHASE_SUMMARY_2026-08-24.md)：`rule-explainer` 已从强制 LLM+MCP 改为 `icoder.governed-rule-explainer.v1`，只解释固定 ICD-10-CN 开发目录可证明的存在性、名称、章节、层级和目录叶子状态。现有 `get_guidelines` 无版本/授权治理，明确禁止作为权威依据；Includes/Excludes、Code First、Use Additional Code、Code Also、组合/排序、医保和地方规则固定标记为未覆盖，绝不从模型记忆补写。冻结 v3 未被改写，v4 追加注册；该阶段真实 loopback HTTP 为 happy/adversarial/reference 各 10/10、stability 60/60，扩大回归 726/726、静态预检 90/90，当时分类为 16 个外部 LLM 必需、1 个可选、9 个纯本地、10 个离线本地基线；严格语义与生产验证仍为 0/26。最新分类见下一段。

Medication Reconciliation 本地收敛见 [`ICODER_GOVERNED_MEDICATION_RECONCILIATION_PHASE_SUMMARY_2026-08-24.md`](ICODER_GOVERNED_MEDICATION_RECONCILIATION_PHASE_SUMMARY_2026-08-24.md)：`med-reconciliation` 已从通用 LLM 模板改为 `icoder.governed-medication-reconciliation.v1`，只从明确 Home/住院 MAR/Discharge 来源提取逐字药物事实和精确 span，按药名字面值及已记录剂量/途径/频次保守分类；旧示例中无证据的肾功能/恢复条件推断已删除。相互作用固定为需要授权药品库但未评估，过敏仅做完全相同字面匹配，所有结果须医师或药师复核。v4 追加合同、7 条关系和 4 条证据绑定已注册；该阶段真实 loopback HTTP 为 happy/adversarial/reference 各 11/11、stability 66/66，宽回归 745/745、静态预检 90/90。当时分类为 15 个外部 LLM 必需、1 个可选、10 个纯本地、11 个离线本地基线；最新分类见下一段。

Nursing Handoff 本地收敛见 [`ICODER_GOVERNED_NURSING_HANDOFF_PHASE_SUMMARY_2026-08-24.md`](ICODER_GOVERNED_NURSING_HANDOFF_PHASE_SUMMARY_2026-08-24.md)：`nursing-handoff` 已从通用 LLM 模板改为 `icoder.governed-nursing-handoff.v1`，只整理明确患者分段中的床位、主要问题、状态、背景、事件、LDA、MAR、检验、待办、安全、升级事项和缺口，并保留逐字 span；最多 10 名患者，不评估 acuity/priority，不推断医嘱、设备状态或未记录的待回报结果，所有结果须接班护士核验。v4 追加合同、5 条关系和证据绑定已注册；最新真实 loopback HTTP 为 happy/adversarial/reference 各 12/12、stability 72/72，宽回归 655/655、字段关系 95/95、证据绑定 34/34、静态预检 90/90。当前分类更新为 14 个外部 LLM 必需、1 个可选、11 个纯本地、12 个离线本地基线；严格语义与生产验证仍为 0/26。

ICU Admission Summary 本地收敛见 [`ICODER_GOVERNED_ICU_SUMMARY_PHASE_SUMMARY_2026-08-24.md`](ICODER_GOVERNED_ICU_SUMMARY_PHASE_SUMMARY_2026-08-24.md)：`icu-summary` 已从外部模型模板改为 `icoder.governed-icu-summary.v1`，只整理明确 ICU 入院字段并保留脱敏输入中的逐字 span；不计算 APACHE II、SOFA、GCS 或死亡风险，不应用生命体征/检验阈值，不执行药物筛查，不生成治疗建议或生产写回。v3 追加合同、6 条关系和证据绑定已注册；该阶段真实 loopback HTTP 为 happy/adversarial/reference 各 13/13、stability 78/78，宽回归 719/719、字段关系 112/112、证据绑定 36/36、静态预检 90/90。当阶段分类为 13 个外部 LLM 必需、1 个可选、12 个纯本地、13 个离线本地基线；严格语义与生产验证仍为 0/26。最新分类见下一段。

Patient Discharge Education 本地收敛见 [`ICODER_GOVERNED_DISCHARGE_EDUCATION_PHASE_SUMMARY_2026-08-24.md`](ICODER_GOVERNED_DISCHARGE_EDUCATION_PHASE_SUMMARY_2026-08-24.md)：`discharge-edu` 已从外部 pure-LLM 模板改为 `icoder.governed-discharge-education.v1`，只逐字整理明确出院字段并绑定脱敏输入 span；不执行患者友好医学释义、结果解释、药物重整、外部知识检索、临床建议或生产写回。v3 追加合同、6 条关系和证据绑定已注册；该阶段真实 loopback HTTP 为 happy/adversarial/reference 各 14/14、stability 84/84，宽回归 744/744、字段关系 138/138、证据绑定 38/38、静态预检 90/90。该阶段分类为 12 个外部 LLM 必需、1 个可选、13 个纯本地、14 个离线本地基线；严格语义与生产验证仍为 0/26。最新分类见下一段。

中国额外 Discharge Summary Structuring 本地收敛见 [`ICODER_GOVERNED_DISCHARGE_SUMMARY_STRUCTURING_PHASE_SUMMARY_2026-08-24.md`](ICODER_GOVERNED_DISCHARGE_SUMMARY_STRUCTURING_PHASE_SUMMARY_2026-08-24.md)：`discharge-summary-structuring` 已从外部 pure-LLM 模板改为 `icoder.governed-discharge-summary.v1`，只逐字重组明确中英文章节并绑定脱敏 span；禁止未标注叙事总结、临床推断、ICD 编码、药物重整、新增医嘱/随访和生产写回。当前合同为追加式 v5；真实 loopback HTTP 为 happy/adversarial/reference 各 15/15、stability 90/90，宽回归 822/822、字段关系 161/161、证据绑定 40/40、静态预检 90/90。当前分类更新为 11 个外部 LLM 必需、1 个可选、14 个纯本地、15 个离线本地基线；严格语义与生产验证仍为 0/26。Corti 公开目录没有同名独立 Agent，邻近对照是 Textgen `corti-discharge-summary` section 与 Patient Discharge Education，不能声称一对一复刻。

官方 A2A JavaScript SDK 包级互操作见 [`ICODER_OFFICIAL_A2A_JS_SDK_INTEROP_PHASE_SUMMARY_2026-08-23.md`](ICODER_OFFICIAL_A2A_JS_SDK_INTEROP_PHASE_SUMMARY_2026-08-23.md)：基线已刷新到稳定 `@a2a-js/sdk@1.0.1` 和 Corti Adapter `0.4.0`，JavaScript `beta.26` 不再返回自建结构客户端，而是返回官方 `ClientFactory`。隔离真实 FastAPI 端到端已跨 Agent Card、ProtoJSON blocking send、持久 JSON-RPC SSE、Context/Task metadata 和 Vercel UI response 通过。A2A 扩大回归 **331/331**、JavaScript **50/50**、Python **51/51**、发布候选验证 **5/5**、静态预检 **81/81**；.NET 仍因本机无 SDK 只记源码/CI 合同。

A2A v1 中断续跑与 AI SDK Adapter 见 [`ICODER_A2A_V1_INTERRUPTED_AI_SDK_ADAPTER_PHASE_SUMMARY_2026-08-23.md`](ICODER_A2A_V1_INTERRUPTED_AI_SDK_ADAPTER_PHASE_SUMMARY_2026-08-23.md)：状态机和迁移 `055` 已增加 rejected、input-required、auth-required；中断 Task 可经 tenant/Agent 绑定的 `taskId` 比较交换恢复，worker 会释放租约并保留加密提示与事件审计。JavaScript `beta.25` 新增 Corti-compatible Vercel AI SDK Adapter，三 SDK 均携带续跑 Task ID。专项 A2A **329 passed、1 skipped**，JavaScript **50/50**，Python **51/51**，迁移专项 **3/3**，静态预检 **81/81**；.NET 因本机没有 SDK 只完成源码与 CI 合同，未记为运行通过。

当前用户可见 Agent 失败关闭规则与证据见 [`ICODER_VISIBLE_AGENT_LAUNCH_CANDIDATE_ENFORCEMENT_PHASE_SUMMARY_2026-08-22.md`](ICODER_VISIBLE_AGENT_LAUNCH_CANDIDATE_ENFORCEMENT_PHASE_SUMMARY_2026-08-22.md)：公共 Hub 和前端均只接受 executable、Provider-resolvable、非 MVP 的开发上线候选；声明公开但不满足门禁的 Pack 会被隐藏并使发布预检失败。

编排器缺失 LLM 的生产失败语义见 [`ICODER_ORCHESTRATOR_LLM_FAIL_CLOSED_PHASE_SUMMARY_2026-08-22.md`](ICODER_ORCHESTRATOR_LLM_FAIL_CLOSED_PHASE_SUMMARY_2026-08-22.md)：Gateway 缺失或无 Provider 时不再生成 `model=stub` 空响应，而是直接返回可重试 503；内部 MedCodER 引擎保持公共 404，mock 公共医学编码保持无假结果的 503。

CDI 后置必需门禁失败关闭见 [`ICODER_CDI_REQUIRED_GATE_FAIL_CLOSED_PHASE_SUMMARY_2026-08-22.md`](ICODER_CDI_REQUIRED_GATE_FAIL_CLOSED_PHASE_SUMMARY_2026-08-22.md)：claim-evidence 与 semantic-necessity 的独立 LLM 降级进入结构化 case 状态，REST/A2A 均 503、不持久化 case、不签名或返回临床结果；REST 失败审计为 tenant-owned 且审计失败继续关闭。

STT 合成协议样例隔离见 [`ICODER_STT_PROTOCOL_FIXTURE_FAIL_CLOSED_PHASE_SUMMARY_2026-08-22.md`](ICODER_STT_PROTOCOL_FIXTURE_FAIL_CLOSED_PHASE_SUMMARY_2026-08-22.md)：固定录音 ID、英文病历转写和完成状态只能在显式 pytest 协议测试中生成；普通 development/Uvicorn 进程即使设置 fixture 环境变量也不能发布伪资源，真实上传、加密持久化及同步/异步转写主链保持不变。

编排专家与 A2A 输出最小披露见 [`ICODER_ORCHESTRATOR_EXPERT_A2A_OUTPUT_ALLOWLIST_PHASE_SUMMARY_2026-08-22.md`](ICODER_ORCHESTRATOR_EXPERT_A2A_OUTPUT_ALLOWLIST_PHASE_SUMMARY_2026-08-22.md)：运行时不再导出 noop/stub 专家成功路径，缺失专家只有 503；21 个 Provider Registry 与 3 条专用简单路由在 attestation 前按 Pack `required_fields + optional_fields` 精确投影，工程诊断只保留在 A2A metadata，mock Medical Coding 继续 503 且无临床结果。

Feedback 训练用途独立授权见 [`ICODER_FEEDBACK_TRAINING_AUTHORIZATION_PHASE_SUMMARY_2026-08-22.md`](ICODER_FEEDBACK_TRAINING_AUTHORIZATION_PHASE_SUMMARY_2026-08-22.md)：反馈或自动评估不再可能被解释为训练许可；只有 tenant owner/admin 可对精确反馈快照授予最长 30 天、固定 `quality_improvement`/`feedback_metadata_only` 的独立资格，更新/删除/retention 自动撤销或清理，Task/Message/模型正文/reason 始终不在范围。

OpenInference Provider telemetry 收敛见 [`ICODER_OPENINFERENCE_PROVIDER_TELEMETRY_PHASE_SUMMARY_2026-08-22.md`](ICODER_OPENINFERENCE_PROVIDER_TELEMETRY_PHASE_SUMMARY_2026-08-22.md)：Provider Registry LLM 与 Connector 的真实 provider/model/token/cost/finish/tool 属性已映射到标准 `llm.*`/`tool.*`，缺失或非法值省略；中国医疗默认仍不导出 `input.value`、`output.value` 或任意临床正文。

专用临床运行时遥测收敛见 [`ICODER_DEDICATED_CLINICAL_TELEMETRY_PHASE_SUMMARY_2026-08-22.md`](ICODER_DEDICATED_CLINICAL_TELEMETRY_PHASE_SUMMARY_2026-08-22.md)：Medical Coding/CDI 现在只从实际调用输出有界 provider/model/token/call-count，mock、降级与不完整 usage 不造值；ASR 以独立加密、无音频/转写正文的 engine/model/latency/status/fallback 合同审计，不冒充 LLM 或虚构 credits。宽矩阵 **1041/1041**、静态预检 **76/76**；同时修复手机号样式 UUID 被 PHI 规则破坏的 A2A Task/Artifact 关联缺陷。

26-Agent 参考质量门禁见 [`ICODER_AGENT_HUB_REFERENCE_QUALITY_GATE_PHASE_SUMMARY_2026-08-22.md`](ICODER_AGENT_HUB_REFERENCE_QUALITY_GATE_PHASE_SUMMARY_2026-08-22.md)：26/26 可见 Agent 现有 Pack 自有合成语义断言，历史真实快乐路径按当时基线离线回放为 **22/26**，四项差异没有被伪装成通过；Evidence Extractor 编码候选守恒、Surgical Registry 明示麻醉/阴性并发症及稀疏证据 v4 已修正。相关宽矩阵 **718 passed、5 skipped、0 failed**，当时部署预检 **77/77**。该基线不是独立临床金标准；其要求的新临时凭证 26-Agent Provider 重测已由 2026-08-25 严格阶段完成，但独立临床金标准限制不变。

逐要求完成性核验见 [`ICODER_COMPLETION_AUDIT_2026-08-21.md`](ICODER_COMPLETION_AUDIT_2026-08-21.md)；Agentic v2 的端点、数据模型、安全、迁移、测试和回滚基线见 [`ICODER_AGENTIC_V2_MIGRATION_DESIGN_2026-08-21.md`](ICODER_AGENTIC_V2_MIGRATION_DESIGN_2026-08-21.md)，Task 归属 Artifact 持久化见 [`ICODER_TASK_OWNED_ARTIFACT_PHASE_SUMMARY_2026-08-22.md`](ICODER_TASK_OWNED_ARTIFACT_PHASE_SUMMARY_2026-08-22.md)，托管 Artifact 对象链见 [`ICODER_MANAGED_ARTIFACT_OBJECT_PHASE_SUMMARY_2026-08-22.md`](ICODER_MANAGED_ARTIFACT_OBJECT_PHASE_SUMMARY_2026-08-22.md)，下载授权隐私加固见 [`ICODER_ARTIFACT_DOWNLOAD_GRANT_PRIVACY_PHASE_SUMMARY_2026-08-22.md`](ICODER_ARTIFACT_DOWNLOAD_GRANT_PRIVACY_PHASE_SUMMARY_2026-08-22.md)；机器委托身份与医疗 scope 的基础证据见 [`ICODER_DELEGATED_MACHINE_SCOPE_PHASE_SUMMARY_2026-08-22.md`](ICODER_DELEGATED_MACHINE_SCOPE_PHASE_SUMMARY_2026-08-22.md)，每 Client Agent/用途委托见 [`ICODER_OAUTH_AGENT_PURPOSE_DELEGATION_PHASE_SUMMARY_2026-08-22.md`](ICODER_OAUTH_AGENT_PURPOSE_DELEGATION_PHASE_SUMMARY_2026-08-22.md)，商业/隐私 Registry 开发合同见 [`ICODER_EXTERNAL_REGISTRY_GATEWAY_PHASE_SUMMARY_2026-08-22.md`](ICODER_EXTERNAL_REGISTRY_GATEWAY_PHASE_SUMMARY_2026-08-22.md)。

## 证据基线

| 范围 | 当前证据 | 判断 |
|---|---|---|
| Agent Hub 用户可见 Agent | 运行矩阵声明可见 26、实际发布 26、门禁排除 0；公开目录 26/26 操作禁用，鉴权租户接口再按组织/部署恢复允许状态；26/26 executable、provider-resolvable、非 MVP、结构性 launch-candidate-ready；21 个 Provider Registry + 5 个专用路由、0 个 legacy fallback；2 个外部 LLM 必需、1 个外部 LLM 可选、23 个纯本地、24 个离线本地基线；最新严格真实 Provider HTTP 为 happy/adversarial/reference 各 26/26、stability 156/156；租户探针/Canary 隔离、过期和失败降级有测试；严格 live-provider 合成语义 26/26、50 次临床校准已执行但未通过、生产就绪 0/26 | 开发路由、租户边界、契约、失败关闭和审计闭环；当前 Pack 自有合成语义通过不冒充完整 Corti/临床能力，CDI、Medical Coding 独立临床质量与医院验收仍开放 |
| Corti 历史 20-Agent 目录 | 20/20 名称映射、开发验证、中国适配声明；临床质量和生产就绪仍 0/20 | 只代表 2026-08-15 目录快照；当前官方 Agentic v2 已转为五类 connector，不能据此声称协议等价 |
| 真实 LLM | 历史隔离 DeepSeek 下 `diagnosis-extractor` 合成最小 Run 4/4 成功；本轮先由单次真实 Run 暴露 v6 输出合同失败，再完成“不猜测编码”的失败关闭修复、28/28 离线回归和唯一一次修复后真实复测。复测 Run `run-71447985-6a29-4c88-9346-d7dd728d2009` 在 32,619 ms、¥0.003083 后通过 v6 并签名，得到已校验 `I21.001`；因 `search_icd` 检索器不可用，Provider 仍为 incomplete 且强制人工复核 | 仅验证一个 Agent 的合成最小链路和失败保护，不能推导临床质量；受治理 ICD 检索仍未闭环 |
| 统一 Run/A2A | 输出白名单、递归 schema、语义约束、跨 Agent attestation、PHI-safe trace | 开发协议链路已闭环 |
| Models | 认证 secret-free LLM 目录、owner/admin 租户选择、乐观锁、配置探针与固定无患者数据实网 Canary；另有组织级 metadata-only 临床模型包治理。开发合成 bundle 可执行 Ed25519/SBOM/内容扫描/隔离聚合探针，并完成三轮 aggregate shadow observation、三态门禁、受控故障停止和 previous-binding 自动回滚；所有动作租户审计 | `runtime_loading_enabled=false` 且 `production_inference_enabled=false`；自动回滚仅为合成 API/事务演练，没有生产 artifact/object store、HSM/KMS、独立 AV/OCR/DLP、OS sandbox、真实模型加载或患者 shadow traffic，也无 Corti 托管模型池、计费、容量或 SLA |
| SDK | JavaScript `beta.46` 97/97、Python `b46` 103/103、.NET `beta.46` 在 net8.0/net10.0 各 85/85；三 SDK 覆盖临床模型包治理、合成 artifact attestation、shadow bind/rollback、聚合评估与历史列表，同时保留预录音、Streams、Agentic v2 与失败关闭合同。.NET 继续提供 netstandard2.0/net8.0/net10.0 资产；候选未发布 | 本机开发 SDK 合同已闭环；仍缺真实 Corti 托管租户双向实测、私有预览 Agent SDK 互操作、真实临床准确率/容量、Linux 发布流水线记录及正式包发布 |
| 部署与安全 | OpenAPI 940,150 bytes、286 paths、314 schemas；源码 Alembic 单 head 为 `060`，fresh head 与 ORM 0 drift。受保护数据库未迁移或切换，仍为 8,536,064 bytes、`2026-08-22 17:16:22`、SHA-256 `2f1e5af0…692877`。合成模型供应链加入 canonical manifest、开发公钥 trust anchor、Ed25519、SBOM、ZIP 防护、开发 scanner、隔离探针和聚合故障/回滚状态机；本阶段静态预检 114/114 | 当前开发源库仍为 `041`，且没有生成 `060` 的完整影子候选或生产 cutover；开发公钥/scanner/子进程与合成回滚不等于 HSM、生产 AV、OS sandbox 或多副本编排。Docker CLI 不可用；Windows/SQLite 不等于 Linux/PostgreSQL、多副本、长期 fuzz 或生产容量。生产仍缺模型/对象存储、AV/OCR/DLP/KMS、真实代理/APM/WAF/SIEM/OTLP、队列、容量/SLA、镜像扫描、真实区域 Provider 与云/医院验收 |

### 2026-08-23 开发环境 26-Agent 执行真实性矩阵

最新一轮在隔离临时 SQLite、空凭据、mock-provider、禁用外部 LLM 与原生 MedCodER 的 ASGI API 上完成两套全量回归，不启动浏览器或 TCP Uvicorn，不读取或使用真实 LLM 密钥。每个 Run 同时查询 trace；CDI 早期 Provider-unavailable 分支原先返回 404 trace，现写入无正文的 `user_message_received` 与失败 `completion` 事件：

| 矩阵 | 完成 | 能力通过 | 安全失败关闭 | 不安全/无效 | 解释 |
|---|---:|---:|---:|---:|---|
| 示例路径 | 26/26 | 24 | 2 | 0 | 24 个本地语义基线在隔离临时数据库中完成；CDI 与 Medical Coding 在外部模型禁用时返回稳定错误原因、`manual_review_required=true` 和受抑制合同输出 |
| 对抗路径 | 26/26 | 24 | 2 | 0 | 注入 canary 未泄漏；模型不可用不计为语义能力通过，2 个外部模型必需项满足独立 fail-closed 安全轴；24 个本地基线均通过严格合同、语义和 trace 检查 |

这不是“26 个 Agent 的临床能力成功率”。pytest 门禁把开发语义轴与安全轴分开，78/78 证明 26 个可见 Agent 的离线示例、对抗和安全约束符合当前 Pack 自有预期；其中 24 个受治理本地基线不等于独立临床金标准，CDI 与 Medical Coding 两个外部模型必需项只允许安全失败关闭。机器证据不把外部模型缺失计为语义通过，也不把确定性目录、抽取或协议路径能力夸大为完整临床质量。

历史 2026-08-13 的 26 条 DeepSeek 对抗响应经当前合同离线重放后只有 10/26 满足现行完整 schema；26/26 场景语义断言及注入 canary 检查仍通过，但这批缓存响应存在合同版本漂移，不能作为当前发布质量证据。所需的轮换凭据、明确预算和全新响应重跑已由 2026-08-25 严格阶段完成；历史缓存仍不能替代新权威目录，也不能证明独立临床质量。原始报告：[`mock_failure_envelope_e2e_20260822`](../../reports/agent_hub/mock_failure_envelope_e2e_20260822/)、[`adversarial_e2e_20260813`](../../reports/agent_hub/adversarial_e2e_20260813/)。

## 差距分层

### 2026-08-23 官方公开文档增量

Corti 当前公开文档显示 Agentic v2 只支持 A2A v1.0，并提供 `registry`、`mcp`、`agent`、`a2a`、`schema` 五类 connector、Context/Task 一级资源、OpenInference trace export、Task/Message feedback、按 Agent usage，以及正式 JavaScript、C#/.NET SDK 和 `@corti/ai-sdk-adapter@0.4.0`；Agent SDK composition 仍标为私有预览。iCoDer 已完成双 binding 的 Send/Stream/Get/List/Cancel/Subscribe、持久 `returnImmediately` Task、五类 Connector graph、trace/feedback/usage/Context/Task/Artifact、标准可恢复事件；`055` 补齐 A2A v1 interrupted/rejected 状态和安全续跑，`beta.26` 又用官方 `@a2a-js/sdk@1.0.1` 关闭本地包级协议互操作。Corti 公开 Core Concepts 仍注明 FilePart 尚未完全支持，公开材料也没有证明 Corti 提供同等托管 AV/DLP 或训练授权流水线，因此后两者只能记为 iCoDer 的安全加固和中国适配，不能冒充一比一验证。仍未关闭的 P0/P1 包括 Corti 托管租户互操作、Provider 原生首 token 临床流、生产对象存储与独立 AV/OCR/DLP、真实 DrugBank/POSOS/隐私搜索/embedding、患者 PHI 权威 consent、Secret Manager、分布式控制面、受权训练内容治理及外部互操作。

### A. 开发环境已完成

| 能力 | iCoDer 当前实现 | 对 Corti 的结论 |
|---|---|---|
| Agent 发现与执行 | 26 个声明公开 Pack 全部通过真实 Provider/专用适配器解析和上线候选门禁；Hub API 与前端只允许 runnable/production 生命周期，metadata-only/stub/MVP/invalid/unknown Provider/actionless Pack 失败关闭；矩阵对被安全隐藏的声明公开 Pack仍使发布失败 | 核心 Agent Hub 工程骨架与可见性失败关闭已对齐 |
| A2A 1.0 Agent Card | 根标准路径公开默认官方 Agent；认证租户路径动态投影自定义/官方 Agent；JSONRPC 与 HTTP+JSON 接口、MIME mode、ETag/缓存和 SDK 均有合同；不暴露 prompt、Connector ID 或内部 metadata | A2A 发现的本地协议缺口已关闭；Corti 托管域名/运行时互操作仍需外部验证 |
| Connector 条件/并行编排 | 条件仅检查已脱敏结构化标量，禁止表达式/自由文本；依赖层有界并行、声明顺序稳定、每节点独立事务与审计、必需节点失败关闭 | 本地服务端 Planner 已可审计；分布式控制面和生产 adapter 仍未验证 |
| 远程 MCP/A2A Connector transport | MCP handshake/session/call；A2A v1 双 binding 与六类操作；Agent Card digest/endpoint pin；DNS 审核地址钉住 socket；同源 redirect 逐跳复验；OAuth2；响应上限；CN/PHI 精确 allowlist；启动/关闭接线 | 开发环境中的受治理远程 transport 已闭环；真实远端互操作、云 Secret Manager、分布式状态和代理网络可达性仍未验证 |
| Registry/内部 Agent Connector | `medical-calculator`、含三个独立 scoped 操作的 `medical-coding`、有界 `interviewing`、线程内兼容 `memory.retrieve` 已接入；持久 `remember/recall/forget` 具备显式用户授权、服务端 actor、用途/tenant/Agent 隔离、加密、PHI 脱敏、注入拒绝、隔离 semantic embedding、模型版本门、到期 purge 与撤销硬删除；PubMed/ClinicalTrials.gov 有固定官方主机；DrugBank/POSOS/Web Search 有固定企业网关合同、凭证/区域/许可/双 opt-in/去标识化门、最小字段投影和非权威复核标记；内部委派有 child Run、trace、签名/人工复核和递归守卫 | 本地执行链、两个公共 Provider、三个外部网关开发合同、三项医疗 scope、每 Client Agent/用途授权及受治理 Semantic Memory 已闭环，ClinicalTrials 单次实网通过；PubMed 实网、真实商业许可/隐私 Provider、患者 PHI 权威 consent、真实 embedding 服务/临床质量、真实多 Agent 模型质量及分布式状态仍未验证 |
| 机器身份与 scoped Connector | 权威 Client ID 只来自 `client_credentials` token；每次复核 token、Client、owner、租户成员、live scope、精确 Agent 白名单和用途授权；撤销立即失败；Run/Connector 双重用途门禁；`048`/`049` 记录 actor、委托主体、scope、purpose 和 Run 用途；Memory 继续拒绝机器 actor | 每 Client Agent/purpose 的本地链路已闭环；外部 token exchange、云 Secret Manager、分布式策略一致性和生产身份系统仍缺失 |
| 输出可信链 | Pack 白名单、递归 schema、enum/范围/关系、证据绑定、上游签名和失败关闭；26 个用户可见 Agent 的 schema-labelled A2A 领域结果执行精确声明字段投影，签名覆盖投影后数据，Provider/延迟等工程信息只在 metadata | 开发阶段可审计性达到上线候选；真实模型临床正确性仍未证明 |
| 多轮上下文 | tenant/agent 绑定 context、脱敏存储、续轮鉴权、删除与 retention | 开发协议能力已覆盖 |
| 中国编码主链 | 诊断/手术抽取、编码员主诊断初稿复核、Medical Coding、Evidence Extractor、Evidence Ranker、ICD-10 Navigator、Code Validation、Compliance、DRG/DIP 的签名链和 CN 编码声明；主诊断复核已接入显式初稿/候选/入院原因/主要治疗/资源使用的精确 span 与集合一致性，Evidence Extractor 已接入显式候选码的中文目录术语/代码字面量精确 span 定位，Evidence Ranker 已接入中文来源/span 文档可追溯性基线，Navigator 已接入哈希固定的 ICD-10-CN 中文术语 Search/一层 Explore，Code Validation 已接入 ICD-10-CN / ICD-9-CM-3 本地成员/可分配性基线 | 中国场景工程链与本地证据审计/导航/初稿复核基线已建立；主诊断自动选择、全病历医学证据抽取与临床支持判断、目录来源/许可、权威病案首页/instructional-note、跨码语义和结算规则仍未完成 |
| 模型安全 | Gateway、旧 LLMService、Embedded Runtime 统一执行 egress policy；pinned 路由不静默 fallback；A2A 深度编排缺失 Gateway/Provider 时直接可重试 503，不生成 stub 模型响应、token、费用或临床输出；CDI claim-evidence/semantic-necessity 后置模型门禁失败时也禁止 case 持久化、attestation 和公共结果 | 开发安全边界已闭合；真实 Provider 质量与可用性仍需外部验证 |
| STT 真实资源与测试样例边界 | 录音上传/读取/删除、加密持久化、同步/持久异步转写、重启恢复、状态与失败关闭已有实现；预录音现支持有界 keyterms、中文口述标点、显式 stereo participant 0/1，以及经受限 `ffprobe`/`ffmpeg` 隔离处理的 WAV/Ogg/WebM/Opus/Vorbis/MP3/FLAC/M4A/AAC 合同。编码双声道被解为两个独立 16 kHz/16-bit mono WAV 后分别识别；有效 FunASR/Whisper 逐句段转换为整数毫秒并加密保存，非法段失败保护，无有效段则明确回退为整声道零边界。10 MIME 的短合成音调同步+持久异步实解码矩阵以两个独立库完成 20/20。三 SDK managed 实时连接具备 ready、类型化事件、token refresh、序号 ACK、连接内去重和有界 client-replay，两个独立 Worker 强制断线恢复已实测。Streams 已有 CONFIG_ACCEPTED、最终 transcript、受治理 Facts、flush/delta usage/usage/end、可选加密留存及租户审计，并以 64,000-byte chunk/32 MiB 会话上限失败关闭；`056`/`057` 提供持久租约、加密 checkpoint/chunks、配置绑定与 session fence。Streams 支持 1–8 声道交错 PCM、精确 participant channel、每声道 transcript/audioEvent/checkpoint、`fixed`/`fast_init` 和有界 keyterms。固定协议样例仍要求 development/test、显式开关和 pytest 三重条件。API 默认不加载原生 STT、不隐式外发到公共 Provider，错误自由文本、转写预览和 keyterm 正文不进入日志/遥测 | 预录音显式双声道 PCM/编码容器、逐格式短音频 E2E、Provider 逐句毫秒传递与 Streams 推荐 PCM 的开发合同、租户隔离、留存、三 SDK 传输、attribution、fast_init、keyterms、跨 Worker 所有权和加密断点恢复切片已关闭，但默认配置没有真实区域 STT/LLM Provider。逐格式矩阵仍只使用合成音调和合成 ASR，不证明真实临床准确率、长音频稳定性或容量。`speakerId=-1` 仍表示无 diarization；时间戳回退也不等于逐句或词级能力。Corti 公开文档未公开 checkpoint 字段/fencing，故私有恢复语义仍未做 head-to-head；生产清理调度、PostgreSQL 多副本和 KMS 轮换也未验证。Corti `/transcribe` 的 commands、formatting、interim、`text/rawTranscriptText` 编辑语义，以及 Streams 多语言、diarization、其他 PCM profile、编码格式 audio events、keyterms 真实准确率、usage/billing、临床准确率和 SLA 仍未复刻或外部验证 |
| 数据库日志隐私 | SQL statement echo 与通用 `DEBUG` 解耦并默认关闭；Cloud 开启即拒绝启动；即使本地显式诊断，应用引擎仍强制隐藏所有绑定参数。两套真实 TCP 验证分别证明默认诊断关闭时 SQL/病历日志为 0，以及显式 statement echo 时 1,796 次参数隐藏标记、病历标记为 0 | 开发日志泄露缺口已关闭；生产日志采集器、SIEM/DLP、保留期和第三方处理者仍需云/医院审计 |
| SQLite 迁移对账 | 默认只读检查；SQLite backup 快照；从空库执行 Alembic 单 head 054 后按共同列复制；源/候选逐表 PHI-safe 指纹；拒绝覆盖；仅显式参数允许在候选创建停用隔离父租户；无自动 cutover。真实开发库副本验证为 6,090 行源数据全保留、候选 0 外键错误、69 张数据表/1,008 列 0 ORM 漂移、源 SHA-256 不变；本轮首次 staging 捕获 054 两个 server default 漂移并在最终候选归零 | 工程对账与候选生成已闭环；6 个隔离租户的真实医院/租户归属、维护窗口、回滚审批和最终切换仍是人工/环境门禁 |
| 可观测性 | Run Trace、SSE resume、跨进程/代理合同、模型部署/版本/决策字段 | 审计能力强于 Corti Console 可见面 |
| Run Trace 复核视图 | Timeline/raw API 提供不含输入输出的安全运行摘要；UI 组合展示状态、模式、延迟、成本、捕获状态和非权威人工复核信号 | 可用于工程审计；复核信号明确不等于临床判定 |
| 配置健康探针 | owner/admin 人工触发部署级无网络探针；超时/熔断状态只读展示；结果写入脱敏审计并回显到租户目录 | 配置状态可审计；真实网络健康、P95 和质量仍需外部验证 |
| 受控实网 Canary | 默认关闭；owner/admin 显式确认；固定无患者数据载荷且禁止额外字段；先持久化审计；单次尝试、¥0.05/8 token/15 秒上限、300 秒冷却；不返回或记录模型正文；三 SDK 与前端已对齐 | 可验证某一时刻的外部连通性；不把结果冒充持续健康、质量、SLA 或权威账单 |
| Agent Run 预算与开发结算 | 双开关、仅本地/开发启用；Provider 调用前预授权，实际成本只结算一次，幂等重放不重复调用/扣费，结算不足时隐藏临床输出并支持充值后幂等重试；API Client 以权威 owner/org 归集并保留 `api_client_id`，只读 token 在输入处理前拒绝，医疗专项 scope 已覆盖；显式陈旧协调只释放孤立 RESERVED，将孤立 SETTLING 转为可重试且不免除成本，活跃 Run 跳过；预授权/结算/协调共用 owner 行 `FOR UPDATE`；前端和三 SDK 已对齐 | 仍是 owner 归集的按用户开发账本；无真实项目级财务账户、支付、发票、退款和对账；PostgreSQL 多副本锁行为待外部实跑 |
| 中国临床资产治理 | ICD-10-CN、ICD-9-CM-3 与 DRG/DIP 风险包有不可变版本、SHA-256/大小、地域、authority/license、生效期、用途限制和回滚冲突门；DRG/DIP v4 Agent 合同由运行时强制 `experimental_unverified`、`billing_authoritative=false`；本地仅可风险复核，cloud/结算/支付/production grouping 失败关闭；feedback 训练资格固定最小范围且不授权临床正文；OpenInference 默认禁止临床输入输出正文；API 认证、UI/Embedded 非结算措辞、类型化 OpenAPI、三 SDK 失败闭合及当前 74/74 部署预检完成 | 治理与防误用框架已完成，但现有本机字典和启发式来源/许可未获验证，训练数据合法性与患者/医院权威许可也未完成，不能作为官方或医院结算/训练依据 |

### B. 仍可在开发环境完成

| 优先级 | 任务 | 验收证据 |
|---|---|---|
| P0 | 设计并实现 A2A v0.3→v1.0 兼容迁移，支持 JSON-RPC/HTTP+JSON 双绑定及 Send/Stream/Get/List/Cancel/Subscribe | **本地开发切片完成**：8 个 v1 路径、持久 Task/event、租约/恢复/协作取消、标准 SSE 与三 SDK 均已实现；`055` 增加 rejected/input-required/auth-required，blocking/Subscribe 在中断态返回，下一条同 tenant/Agent `taskId` 以 CAS 恢复 working，终态不可恢复，Task status prompt 和结果加密保存。官方 `@a2a-js/sdk@1.0.1` 已跨真实 iCoDer Agent Card、blocking 与 streaming 路由；A2A 扩大矩阵 331/331、迁移专项 3/3、预检 81/81。仍缺生产队列/多副本、跨进程恢复竞争实压、外部 Provider 物理中止/费用保证、.NET 本机实跑和 Corti 托管租户互操作 |
| P0 | 实现 `registry`、`mcp`、`agent`、`a2a`、`schema` 五类 connector 资源模型 | **五类启动执行入口、两个公共 Provider、三个外部企业网关开发合同、受治理 Semantic Memory 和机器 scoped 医疗操作的开发切片完成，生产 adapter 仍开放**：`044` Connector 三表与 graph；`047` 用户 consent/retention；`048`/`049` machine owner、scope、Agent/用途授权和审计；MCP/A2A transport、本地 registry、PubMed/ClinicalTrials.gov、DrugBank/POSOS/Web Search 网关和内部 Agent 已接线。Semantic Memory 阶段完成隔离 HTTPS contract、加密向量、真实 TCP 1/1、Cloud 失败关闭、健康/API/SDK 合同和部署预检 54/54；JS 41/41、Python 48/48。开发库 `041` 已生成并验证独立 `049` 候选，但尚未进行租户归属复核或切换。仍缺 PubMed 合规邮箱实网、真实 DrugBank/POSOS 许可及供应商映射、真实隐私搜索 Provider、患者 PHI 权威 consent、真实 embedding 服务和检索质量、真实 Secret Manager、分布式控制面、多副本和外部互操作验证 |
| P1 | 补齐 context task 集合、OpenInference trace export 与 task/message feedback | **一等资源、Provider Registry 与专用 Medical Coding/CDI 遥测、ASR 内容无关审计、增量 Artifact、托管对象与 feedback 训练独立授权开发切片完成，生产存储/训练内容治理仍开放**：`050`–`052` 覆盖 Context/Task/Artifact、标准可恢复事件和精确加密 chunk；`053` 新增隔离扫描托管对象；`054` 增加 owner/admin、精确快照、固定质量改进/metadata-only、最长 30 天、更新即撤销的训练资格，三 SDK 均有调用面。Provider Registry、Medical Coding 与 CDI 只映射实际观察到的 provider/model/token/cost/call-count；ASR 只记录 engine/model/latency/status/fallback，不伪造 LLM token 或 credits；输入输出正文仍禁止。当前实现是数据库开发模拟；训练授权明确不含 Task/Message/模型正文/reason，也不存在训练导出。仍缺生产对象存储、独立 AV/OCR/DLP、KMS、真实边缘/APM/WAF/SIEM/OTLP 隐私验收、未经终态验证的原生 token 低延迟公开流、ASR 公开 usage/真实质量/diarization/词级时间戳、患者/医院权威训练许可、训练语料去标识/导出/lineage、.NET CI 和外部多副本验证 |
| P1 | 按 Agent 用量、SDK 与 Agent SDK composition | **开发切片完成**：按租户/Agent 日用量、TypeScript/Python composition、.NET `netstandard2.0`/`net8.0`/`net10.0` 请求面、`.NET Framework 4.6.2` 最低消费者编译和 JS AI SDK Adapter 均有本地合同；Adapter 支持 context/task continuity、UI stream、生命周期 callbacks、MCP credential DataPart、同源 Bearer fetch，并以无 `ai` 强依赖方式打包。仍缺生产账单对账、服务端分布式编排、真实 Linux CI 运行记录、Corti 托管 SDK/Adapter 双向互操作，以及 Corti 私有预览 Agent SDK 的实测 |
| P1 | 在受控预算下对 26 个 Agent 执行真实模型合成快乐/对抗/重复回归，统一记录 schema、失败原因、P50/P95 和成本 | **开发环境严格合成调用门禁已完成，顶层 wrapper 终态诊断仍待下一次真实运行闭环**：`212957` 权威顶层报告为 happy/adversarial/reference 各 26/26、stability 156/156、整体 P50/P95 0.449/4.981 秒、成本覆盖 150/156；`224918` fresh 源级报告在补齐 CDI 安全门计量和 2 路并发后仍为 26/26、156/156，整体 P50/P95 0.753/6.348 秒、成本覆盖 156/156。CDI P50/P95 20.378/26.662 秒、成本 6/6，Medical Coding 5.511/6.688 秒、成本 6/6，30/10 秒开发预算均通过。有效 bundle/Runtime Matrix 已落盘，但 wrapper 在顶层 evidence 前失败且旧版本未保存精确异常；forensic recovery 不将其提升为顶层权威成功。Provider 发票对账、独立 50 次临床校准、Corti head-to-head、医院验收和生产验证继续开放 |
| P1 | 在用户明确授权和隔离凭据下执行一次受控实网 Canary，并归档不含正文/密钥的证据 | 工程合同、预算、超时、冷却、审计、租户可见结果和 48/48 离线反例已完成；尚未把真实调用伪装成已执行 |
| P1 | 闭环隔离 MedCodER Worker 的 API 接线与真实检索 E2E | **开发 HTTP 合同切片完成，Linux 原生质量仍外部**：显式 Compose overlay 同时接线 Backend/Worker、要求独立 token、等待 readiness 并禁用 API 原生栈；远端健康覆盖 MCP 权威门；两个真实 Uvicorn 子进程经 TCP 完成双编码、401、Backend health/version 和 MCP `search_icd`，扩大回归 93/93、静态预检 52/52。fixture 不加载原生 ML；本机无 Docker，仍缺 Linux 镜像、真实 BGE/FAISS 预热/检索、SBOM、容量、故障注入和质量评测 |
| P1 | 建立不依赖危险 Windows 原生栈的中国临床编码监督基线 | **开发评测、治理控制面、合成 artifact 供应链及无患者流量的 observation/停止/自动回滚状态机已完成，Runtime 提升仍失败关闭**：1,800 例五折 OOF 的泄漏/目录门、聚合指标与三次稳定复跑已完成；组织级 metadata-only manifest/四眼审批、签名合成 bundle 的 attestation/shadow-only 选择，以及三轮聚合观察和三类受控故障均已实现。但完整集合仍仅 1.72%，同一训练工作簿 OOF 不是外部测试集；当前 CCL 包因许可、再分发、独立 gold、医院与云授权缺失被激活门明确阻断。下一步开发工作是生产 adapter/分布式 worker/告警接口与长稳模拟；真实模型加载、患者 shadow traffic、Corti 同病例盲评和医院验收必须等待外部证据，不能直接把 CCL fixture 打进产品 |
| P1 | 在 PostgreSQL 隔离环境验证并发预授权/结算锁语义 | OAuth owner 归集、scope 门禁、显式崩溃协调及 `FOR UPDATE` SQL 合同已完成；仍缺真实多副本并发证明 |
| P2 | 为 RunTrace 组合视图增加稳定的视觉快照/浏览器 E2E | 当前 API、前端类型、生产构建和 132/132 前端测试已完成，仍缺视觉回归证据；本机已有浏览器内存崩溃风险，未强行启动 Chromium |
| P2 | 在 Linux CI 复跑 .NET 三资产/双原生运行时/最低消费者、PostgreSQL 多副本、Docker SBOM/漏洞和 Nginx SSE | .NET PR/发布工作流已经配置三资产、net8/net10 测试及 netstandard2.0/net462 消费者阻断门；本机新鲜证据已通过，但真实 GitHub Linux job 运行记录、PostgreSQL/Docker/Nginx 外部证据仍不能由 Windows 本机替代 |
| P2 | 在取得合法数据包后实现权威国家/省市/医院规则导入适配与差异回放 | 治理 manifest、完整性、用途限制、回滚/冲突测试已完成；实际来源、许可、生效版本和地区规则必须由权利方/医院提供，不能在代码中伪造 |

### C. 必须由外部环境完成

| 能力 | 当前差距 | 外部门禁 |
|---|---|---|
| Corti Models 托管等价 | iCoDer 仅能登记外部/本地部署；无托管模型池、个人模型 Key 生命周期、在线健康、容量和 SLA | 云账户、KMS、真实 Provider 合同、容量和安全审计 |
| Corti 计费 | iCoDer 有逐 Run 开发预授权/幂等结算和模拟账本，但无真实项目余额、发票、退款和财务结算 | 支付/财务系统、商业合同和账单验收 |
| 商业/隐私 Registry | DrugBank/POSOS/Web Search 已有受治理网关、失败关闭、审计和真实 TCP 开发合同，但尚未连接任何获许可的真实供应商 | DrugBank/POSOS 商业许可与字段映射、搜索供应商 DPA/区域评估、KMS、生产证书/DNS、药师/临床 reviewer 验收 |
| 临床质量 | 4/4 仅为一个 Agent 的合成最小 Run，不是 26-Agent 临床质量证明 | 独立临床 reviewer、双盲标注、医院验收、误差/偏差评估 |
| 医院互操作 | 没有真实 HIS/EMR、FHIR/接口网关、院内身份和网络隔离验收 | 医院 IT、互操作测试、数据治理和上线审批 |
| 中国合规 | 有 CN 区域默认拒绝出境、脱敏和审计工程，但无真实等保、个保/数安/网安评估 | 法务、测评机构、医院制度、备案/认证和数据出境评估 |
| 中国编码/DRG-DIP 权威数据 | 本机 ICD 目录已哈希固定但来源/许可未验证；DRG/DIP 仅为非权威开发风险启发式，不计算官方分组、权重、分值或支付 | 国家/地方医保、医院或合法供应方提供的版本化规则与授权；地区生效期、结算引擎对照、临床/医保 reviewer 验收 |
| 生产运维 | 无真实多区域、灾备、容量、值守、告警和 SLA 演练 | 云基础设施、SRE、备份恢复、渗透测试和供应链审计 |

## Corti 只读产品对照

2026-08-15 在用户已登录的 Corti Console 只读看到：Corti Models 页面提供欧洲基础设施托管 frontier models、EU 数据驻留/GDPR 说明、`npx @corti/cli models init`、浏览器认证生成个人模型 Key 和项目计费入口。iCoDer 当前已经补齐租户模型选择、精确路由和逐 Run 审计，但没有把运维部署伪装成上述托管服务。

## 当前结论

iCoDer Agent Hub 已达到“开发环境可审计上线候选”的工程状态，但尚未达到“完整复刻 Corti 托管能力”或“可直接用于临床生产”。26 个声明公开 Pack 全部 executable、Provider-resolvable、非 MVP、结构性 launch-candidate-ready；当前为 2 个外部 LLM 必需、1 个可选、23 个纯本地、24 个离线本地基线。完整顶层权威 `212957` 与新源级 `224918` 均以真实 loopback HTTP 和真实 Provider 证明 happy/adversarial/reference **26/26**、stability **156/156**，严格 26-Agent live-provider 合成语义为 **26/26**；后者额外证明成本覆盖 156/156 和专用 Agent P95 预算，但 wrapper 顶层终态失败未被提升为新的 phase success。新增 CCL 五折 OOF 已把本地中国编码“监督开发基线未测量”关闭，并给出 89.56% 主诊断、50.79% 全诊断 F1、66.38% 有 gold 主手术和 83.80% 全手术 F1；但它没有进入 Runtime，完整集合仅 1.72%，也没有外部 untouched cohort。50 次临床校准已执行但未通过质量门、生产就绪仍为 **0/26**。Triage 的显式问卷路径、主诊断初稿的集合/证据一致性复核、Clinical Guidelines 的声明规则比较、Clinical Education 的批准来源原句装配及其余受治理能力，都不证明对话答案抽取、最终临床分诊、主诊断自动选择、外部指南检索/验证、生成式临床推理、完整临床/护理/药学/编码语义质量或 Corti 模型等价。

已闭环的工程面在既有失败域、Trace、OAuth、Agentic v2、Connector、Artifact、feedback 与最小遥测基础上，现包含 24 个确定性或受治理离线本地基线、CDI/Medical Coding 真实 Provider 路径，以及 Clone→Customize→Run/A2A 的项目身份与源 Runtime 双身份执行。本轮先增加完全本地、无病例级落盘的 CCL 监督 OOF 评测器，再补齐组织级临床模型包 metadata-only manifest、不可变版本、四眼审批、激活/切换/严格 rollback、三 SDK 与 Console；静态预检提升到 **112/112**。控制面仍明确 `runtime_loading_enabled=false`，不增加 Runtime 的 24 个离线基线计数，也不把开发审批状态冒充临床批准。此前完整后端、前端、三 SDK、A2A、Streams 和迁移证据继续保留各自阶段口径，不能用本轮范围覆盖，也不能据此声称 Corti 托管语义或生产质量等价。

受治理的 **50 次 CDI/Medical Coding 临床校准已执行一次，但没有通过质量门**。原报告为 `invalid_or_incomplete_evidence`；评测器使用错误输入造成 21 个数字溯源假阳性，修正口径后的原响应只读重评分为 41 个 capability pass、8 个 unsafe/invalid、1 个 safe-fail-closed。随后已完成双编码系统请求、双语中国目录检索、定量值与证据锚定、无证据不发布编码、PHI 临床词误删和种子目录成员校验修复；这些改动仍需新临时 Key 重新执行 50 次才能形成新鲜证据。CCL OOF 只使用现有训练工作簿，不能替代这 50 次外部 Provider 校准，更不能替代独立 CDI reviewer/编码金标准和相同授权病例的 Corti head-to-head。故临床质量、Corti 等价、医院验收和生产就绪继续为未证明。工程侧还需关闭编码目录的部署打包与来源/许可、权威中国病案/医保/DRG/DIP 规则、真实医院工作流、语音临床质量、Connector 实网、PostgreSQL 多副本、Secret Manager/KMS、容量/SLA/灾备及法务认证。5 个 metadata-only/stub 与 1 个 internal Pack 保持隐藏。受保护开发库仍不得自动迁移或切换；真实国家/地区/医院规则、合法授权、医院、云、法务、认证和独立临床验收必须保持未通过，直到取得外部证据。

详细阶段证据：[`ICODER_MANAGED_ARTIFACT_OBJECT_PHASE_SUMMARY_2026-08-22.md`](ICODER_MANAGED_ARTIFACT_OBJECT_PHASE_SUMMARY_2026-08-22.md)、[`ICODER_INCREMENTAL_ARTIFACT_STREAM_PHASE_SUMMARY_2026-08-22.md`](ICODER_INCREMENTAL_ARTIFACT_STREAM_PHASE_SUMMARY_2026-08-22.md)、[`ICODER_AGENTIC_V2_CONTEXT_RESOURCES_PHASE_SUMMARY_2026-08-22.md`](ICODER_AGENTIC_V2_CONTEXT_RESOURCES_PHASE_SUMMARY_2026-08-22.md)、[`ICODER_DATABASE_LOG_PRIVACY_PHASE_SUMMARY_2026-08-22.md`](ICODER_DATABASE_LOG_PRIVACY_PHASE_SUMMARY_2026-08-22.md)、[`ICODER_AGENT_FAILURE_ENVELOPE_PHASE_SUMMARY_2026-08-22.md`](ICODER_AGENT_FAILURE_ENVELOPE_PHASE_SUMMARY_2026-08-22.md)、[`ICODER_SEMANTIC_MEMORY_PHASE_SUMMARY_2026-08-22.md`](ICODER_SEMANTIC_MEMORY_PHASE_SUMMARY_2026-08-22.md)、[`ICODER_EXTERNAL_REGISTRY_GATEWAY_PHASE_SUMMARY_2026-08-22.md`](ICODER_EXTERNAL_REGISTRY_GATEWAY_PHASE_SUMMARY_2026-08-22.md)、[`ICODER_MEDCODER_REMOTE_HTTP_PHASE_SUMMARY_2026-08-22.md`](ICODER_MEDCODER_REMOTE_HTTP_PHASE_SUMMARY_2026-08-22.md)、[`ICODER_OAUTH_AGENT_PURPOSE_DELEGATION_PHASE_SUMMARY_2026-08-22.md`](ICODER_OAUTH_AGENT_PURPOSE_DELEGATION_PHASE_SUMMARY_2026-08-22.md)、[`ICODER_DELEGATED_MACHINE_SCOPE_PHASE_SUMMARY_2026-08-22.md`](ICODER_DELEGATED_MACHINE_SCOPE_PHASE_SUMMARY_2026-08-22.md)、[`ICODER_GOVERNED_MEMORY_CONNECTOR_PHASE_SUMMARY_2026-08-22.md`](ICODER_GOVERNED_MEMORY_CONNECTOR_PHASE_SUMMARY_2026-08-22.md)、[`ICODER_GOVERNED_PUBLIC_REGISTRY_PHASE_SUMMARY_2026-08-22.md`](ICODER_GOVERNED_PUBLIC_REGISTRY_PHASE_SUMMARY_2026-08-22.md)、[`ICODER_LOCAL_REGISTRY_INTERNAL_AGENT_CONNECTOR_PHASE_SUMMARY_2026-08-22.md`](ICODER_LOCAL_REGISTRY_INTERNAL_AGENT_CONNECTOR_PHASE_SUMMARY_2026-08-22.md)、[`ICODER_GOVERNED_CONNECTOR_TRANSPORT_PHASE_SUMMARY_2026-08-22.md`](ICODER_GOVERNED_CONNECTOR_TRANSPORT_PHASE_SUMMARY_2026-08-22.md)、[`ICODER_AGENTIC_TRACE_FEEDBACK_PHASE_SUMMARY_2026-08-22.md`](ICODER_AGENTIC_TRACE_FEEDBACK_PHASE_SUMMARY_2026-08-22.md)、[`ICODER_A2A_V1_DURABLE_ASYNC_TASK_PHASE_SUMMARY_2026-08-22.md`](ICODER_A2A_V1_DURABLE_ASYNC_TASK_PHASE_SUMMARY_2026-08-22.md)、[`ICODER_RUN_TRACE_BILLING_PHASE_SUMMARY_2026-08-21.md`](ICODER_RUN_TRACE_BILLING_PHASE_SUMMARY_2026-08-21.md)。
