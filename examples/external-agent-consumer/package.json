{
  "name": "icoder-external-agent-consumer",
  "version": "1.0.0",
  "description": "iCoDer External Agent Consumer — Sprint 2 Goal F verification (pure REST + SDK, no internal imports, no DB)",
  "private": true,
  "type": "module",
  "scripts": {
    "rest": "node run-agent.mjs --mode rest",
    "sdk": "node run-agent.mjs --mode sdk",
    "start": "node run-agent.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "comment": "No runtime deps — Node 18+ has global fetch. @icoder/sdk is optional (loaded from local workspace path; falls back to pure REST if absent)."
}
