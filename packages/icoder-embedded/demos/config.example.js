// iCoDer Partner Demo — Static Config Template
// Phase 7 Gate 1 (§6.3)
//
// This file documents the shape of window.icoderConfig that the demo HTML
// reads. In production, the backend generates this dynamically via
// GET /examples/config.js from env vars (see .env.example).
//
// To run a demo WITHOUT the backend mount (e.g. open the HTML directly
// via file://), copy this file to config.js in the same directory,
// uncomment window.icoderConfig, fill in your values, and edit the demo
// HTML to load ./config.js instead of /examples/config.js.

/*
window.icoderConfig = {
  // Backend base URL where the partner's iCoDer tenant is hosted.
  baseUrl: 'http://localhost:8000',

  // Default agent to invoke (used as form-default; the partner user can
  // still override via the sidebar select).
  agentRef: 'medical-coding-agent',

  // API Client ID (visible to browser; low sensitivity).
  // Gate 5 will provision real api_clients.
  apiClientId: '',

  // Organization / tenant ID (multi-tenant routing in cloud mode).
  organizationId: '',

  // Demo asset version (set by the backend; for traceability).
  demoVersion: '1.0.0-phase7-gate1',
};
*/
