# Phase A1A Gate 0 Addendum — Sub-gate 0C
## Untracked Files Classification

> Classifies all 60 untracked working-tree items (per
> `git status --porcelain | grep '^??'`) into 4 categories. The 60-item
> count collapses directories — actual file count is 2151.
>
> No untracked item blocks Gate 0 Addendum closure. `.audit-chrome-profile/`
> gitignore addition is recorded as a Gate 1 mandatory action.

Spec reference: Phase A1A charter §6.5 (Gate 0 Addendum sub-gate 0C).

Artifacts under `reports/phase-a1a/`:
- `A1A_GATE0C_UNTRACKED_FILES_CLASSIFICATION.md`  (this report)
- `untracked_files_classification.json`             (machine-readable)

---

## §1. Working tree snapshot

| Property | Value |
|---|---|
| Current branch | `phase-a1a/emergency-containment` |
| Master hash | `c147d015455017bc1d8420cbdbd813b3b8ec23ce` (unchanged) |
| Tag target | `64590fa` (Commit C, A1A branch anchor) |
| `git status --porcelain \| grep '^??'` | 60 entries |
| `git ls-files --others --exclude-standard` | 2151 files |

The 60-item count is what `git status` collapses — directories with many
untracked files count as one entry. The 2151 file count is the raw
expansion.

---

## §2. Four categories

### Category A — WILL_NOT_COMMIT (transient local state)

**1 entry, 2010 files.** Browser automation artifacts.

| Path | Files | Provenance | Risk |
|---|---|---|---|
| `.audit-chrome-profile/` | 2010 | Playwright MCP / Chrome user-data-dir from Phase 7 Gate 10/11/12/13 and A1A Gate 0 browser walkthroughs | Contains browser state (session cookies, cached auth tokens, browsing history). MUST NOT commit. |

**Gate 1 mandatory action**: add `.audit-chrome-profile/` to `.gitignore`.

### Category B — PRE_EXISTING_PHASE_REPORT (leave as-is)

**46 entries.** Pre-existing audit reports and docs from earlier phases.
Phase A0.1R Bucket B scoped exactly which audit files to track in Commit
B (`606dc5d`); these remained untracked by design. A1A Gate 0 Addendum
does NOT modify them and does NOT commit them.

Notable subsets:
- **Phase A0 main audit (GATE1-GATE14 + 26A-26I)** — 28 reports, superseded by Phase A0.1R
- **Phase A0.1 (REFUTED)** — 16 reports under `phase-a0.1/`, explicitly refuted in Phase A0.1R
- **Phase A0** — `phase-a0/` directory
- **Phase 6 / Phase 7** — `reports/phase6/` (9 items) + `reports/phase7/` (24 items)
- **Old audit scripts** — `validate_phase_a0.py`, `validate_phase_a0_1.py`, `build_maturity_v2.py`, `build_parity_v2_2.py`, `stage_phase_a0_1_commit.sh` (superseded by `*_v3.py` and `validate_phase_a0_1r.py` tracked in Commit B)
- **Misc** — `docs/audit/icoder_comprehensive_product_audit.txt`, `docs/corti_parity/phase7_gate13a/e2e_widget_interactive.png`, `reports/comprehensive-audit/evidence_manifest.json`, `reports/comprehensive-audit/evidence/corti-foundation/`, `reports/comprehensive-audit/evidence/git/`, `scripts/audit/__pycache__/`
- **Phase A0.1R post-tag validator output** — `reports/comprehensive-audit/phase-a0.1r/evidence/post_tag_validation_report.json` (regenerated by validator on each run)

These items are kept untracked on the A1A branch. No action required.

### Category C — CURRENT_A1A_GATE0_OUTPUTS (commit on A1A branch)

**12 entries.** A1A Gate 0 + Gate 0 Addendum artifacts produced today.
These are deliverables of this phase.

| File | Source |
|---|---|
| `reports/phase-a1a/A1A_GATE0_FROZEN_BASELINE_ENTRY.md` | Gate 0 main report |
| `reports/phase-a1a/tag_lineage_verification.json` | Gate 0 outputs 1-7 |
| `reports/phase-a1a/negative_fixture_coverage.json` | Gate 0 output 8 v1 (11 fixtures) |
| `reports/phase-a1a/negative_fixture_coverage_v2.json` | Gate 0 Addendum (12 fixtures) |
| `reports/phase-a1a/credential_invalidation_verification.json` | Gate 0 output 9 |
| `reports/phase-a1a/git_object_secret_scan.json` | Gate 0 output 10 |
| `reports/phase-a1a/baseline_bundle_receipt.json` | Gate 0 output 12 |
| `reports/phase-a1a/a1a_entry_validation.json` | Gate 0 output 15 + hard checks |
| `reports/phase-a1a/A1A_GATE0B_HTTP_AUTH_REJECTION.md` | Sub-gate 0B report |
| `reports/phase-a1a/http_auth_rejection.json` | Sub-gate 0B JSON |
| `reports/phase-a1a/untracked_files_classification.json` | Sub-gate 0C (this) |
| `reports/phase-a1a/A1A_GATE0C_UNTRACKED_FILES_CLASSIFICATION.md` | Sub-gate 0C (this MD) |

**Action**: commit on `phase-a1a/emergency-containment` at Gate 1
kickoff (after Gate 0 Addendum closes).

### Category D — NEEDS_A1A_GATE1_TRIAGE (modified tracked files + new scanner)

**2 entries.** A1A Gate 0 changes that touch tracked files.

| Path | Status | Summary |
|---|---|---|
| `scripts/audit/a1a_gate0_scan_git_objects.py` | untracked (new) | Scanner that produced `git_object_secret_scan.json` |
| `scripts/audit/run_negative_fixtures_a0_1r.py` | modified (tracked in `606dc5d`) | Added NF12 fixture (+21 lines): patches validator fingerprint in-memory, modifies README.md in try/finally. No persistent secret or product code change. |

**Action**: stage and commit at Gate 1 kickoff.

---

## §3. Diff summary of modified tracked file

```
$ git diff scripts/audit/run_negative_fixtures_a0_1r.py
+    # --- NF12: worktree.no_secret — inject a leak marker into a tracked file ---
+    # Patches SECRET_FINGERPRINT_SUBSTRING in-memory to a benign test marker, appends
+    # the marker to an existing tracked file (README.md, not in path exclusions),
+    # and verifies check_no_secret_in_worktree catches it. File is restored in finally.
+    print("\n[NF12] worktree.no_secret — inject leak marker into tracked README.md")
+    import validate_phase_a0_1r as _v_mod
+    benign_marker = "A1A_GATE0_ADDENDUM_NF12_BENIGN_LEAK_MARKER_x9k2q4z7"
+    original_fingerprint = _v_mod.SECRET_FINGERPRINT_SUBSTRING
+    target = REPO_ROOT / "README.md"
+    backup_bytes = target.read_bytes() if target.exists() else None
+    try:
+        _v_mod.SECRET_FINGERPRINT_SUBSTRING = benign_marker
+        with open(target, "a", encoding="utf-8") as fh:
+            fh.write("\n" + benign_marker + "\n")
+        result_nf12 = _v_mod.check_no_secret_in_worktree()
+        all_pass &= expect_fail("NF12", result_nf12, "secret found")
+    finally:
+        if backup_bytes is not None:
+            target.write_bytes(backup_bytes)
+        _v_mod.SECRET_FINGERPRINT_SUBSTRING = original_fingerprint
```

**Why this is safe**:
- The marker is a benign string (`A1A_GATE0_ADDENDUM_NF12_BENIGN_LEAK_MARKER_x9k2q4z7`), NOT the secret.
- The validator's `SECRET_FINGERPRINT_SUBSTRING` is patched **in-memory only** (process-local Python attribute).
- README.md is modified and restored in a `finally` clause — even if the test throws, the file is restored.
- No persistent state changes; no product code modified.

---

## §4. Summary table

| Category | Entries | Files | Action |
|---|---|---|---|
| A — WILL_NOT_COMMIT | 1 | 2010 | Add to .gitignore in Gate 1 |
| B — PRE_EXISTING_PHASE_REPORT | 46 | ~120 | Leave untracked |
| C — CURRENT_A1A_GATE0_OUTPUTS | 12 | 12 | Commit on A1A branch at Gate 1 kickoff |
| D — NEEDS_A1A_GATE1_TRIAGE | 2 | 2 | Stage and commit at Gate 1 kickoff |
| **Total** | **60** | **2151** | |

---

## §5. Charter compliance

| Charter rule | Status |
|---|---|
| All untracked files classified | ✅ 60/60 |
| No untracked file contains the secret | ✅ verified by Gate 0 scanner (git_object_secret_scan.json) |
| No untracked file is a product-code change | ✅ Categories A/B/C/D contain zero product-code changes |
| `.audit-chrome-profile/` not committed | ✅ |
| Pre-existing reports left untouched | ✅ |
| A1A Gate 0 outputs grouped for commit | ✅ Category C+D = 14 files ready for Gate 1 kickoff commit |

---

## §6. Verdict

```
============================================================================
SUB-GATE 0C: UNTRACKED_FILES_CLASSIFIED
============================================================================

  Total untracked entries : 60 (2151 expanded files)
  Category A (transient)  : 1 entry / 2010 files → gitignore in Gate 1
  Category B (pre-existing): 46 entries / ~120 files → leave untracked
  Category C (A1A Gate 0)  : 12 entries / 12 files → commit at Gate 1 kickoff
  Category D (modified)    : 2 entries / 2 files → stage + commit at Gate 1

  No item blocks Gate 0 Addendum closure.
  .audit-chrome-profile/ gitignore addition recorded as Gate 1 mandatory action.

NEXT: Sub-gate 0D — Git Bundle Verify and Restore
============================================================================
```

End of Sub-gate 0C.
